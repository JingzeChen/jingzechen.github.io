source:
  post: _posts/book-notes/operating-system/ospp/2026-08-02-ospp-ch03-programming-interface.md
  body_sha256: daff4727fd61c1f65e7bd94f1737b133c51b6327708e9a54ba461a35dff4fcfb
  extraction: deterministic-extractive-v4
  fidelity: extractive
overview:
  kind: summary
  minutes: 10
  question: 原文围绕「全章主线」建立了怎样的核心认识？
  question_source: 全章主线
  thesis: 内核驱动拥有完整权限。普通 bug 可静默改写用户/内核数据，恶意驱动可直接植入内核病毒。原书引用研究称约 90% 系统崩溃来自驱动 bug，突出驱动隔离价值；该比例是书中时代研究，不应视为今天所有系统的恒定统计。
  thesis_source: 3.5 操作系统结构
  concepts:
  - term: 全章主线
    meaning: 约十二个系统调用即可描述实用的进程管理与 I/O 接口，其他主要能力也只需另一小组调用。这些 UNIX 接口从 20 世纪 70 年代延续至今，说明简单而可组合的抽象具有很长寿命。
    source_heading: 全章主线
  - term: 3.1 进程管理（Process Management）
    meaning: UNIX 还允许进程向另一进程发即时通知，用于终止、暂停、恢复、定时器等。接收者未安装 handler 时，内核执行默认动作。这里沿用第
      2 章 upcall 机制。
    source_heading: 3.1 进程管理（Process Management）
  - term: 3.3 案例：实现 Shell
    meaning: 内核只需提供进程和 I/O 原语，Shell 自己负责读取命令、解析语法、创建进程、设置描述符和等待任务。它不需要特权，也不需要为每种组合修改内核。
    source_heading: 3.3 案例：实现 Shell
  - term: 3.5 操作系统结构
    meaning: 内核驱动拥有完整权限。普通 bug 可静默改写用户/内核数据，恶意驱动可直接植入内核病毒。原书引用研究称约 90% 系统崩溃来自驱动 bug，突出驱动隔离价值；该比例是书中时代研究，不应视为今天所有系统的恒定统计。
    source_heading: 3.5 操作系统结构
  - term: 章末练习详解
    meaning: 此时内核无需阻塞，只复制退出状态、回收残余 PCB/PID 信息并返回。若子仍运行，阻塞；若 PID 不是子进程或状态已被其他 wait
      收集，通常立即返回错误而不是成功。
    source_heading: 章末练习详解
  - term: 复习检查清单
    meaning: 能说明作业控制为何需要进程组、终端前台组和 SIGCHLD。
    source_heading: 复习检查清单
  logic:
  - 全章主线
  - 3.1 进程管理（Process Management）
  - 3.2 输入/输出（Input/Output）
  - 3.5 操作系统结构
  - 3.6 总结与未来方向
  - 全章总结：本章的分析方法
  - 复习检查清单
  sections:
  - title: 全章主线
    source_heading: 全章主线
    paragraphs:
    - 第 2 章回答内核怎样提供受限执行；本章进一步问：操作系统应向应用提供哪些功能，这些功能应放在内核、用户库、普通用户程序，还是独立服务进程中？
    - 操作系统通常还需提供线程、内存、文件系统、网络、图形和认证。本章重点只讲进程管理与 I/O；线程、内存和持久存储留到后续章节。
    - 约十二个系统调用即可描述实用的进程管理与 I/O 接口，其他主要能力也只需另一小组调用。这些 UNIX 接口从 20 世纪 70 年代延续至今，说明简单而可组合的抽象具有很长寿命。
    - 进程数量、引用计数、缓冲区和调用成本的推导，以及 C 接口示例，直接放在相关系统调用与案例之后；平台差异则在对应接口处说明，使抽象、实现和使用方式连续衔接。
  - title: 开篇：功能放在哪里？四种实现位置
    source_heading: 开篇：功能放在哪里？四种实现位置
    paragraphs:
    - UI 控件等功能可链接到每个应用。函数调用便宜，不需跨权限边界，不同应用还可选择不同版本。缺点是库与应用共享命运，恶意应用可绕过库检查。
    - 应用通过系统调用/IPC 请求独立服务进程，例如窗口管理器。服务可隔离崩溃和独立升级，但请求需要应用→内核→服务器→内核→应用等额外切换。
    - 普通函数调用最便宜，进入内核更贵，经内核转发到服务器又增加 IPC 和调度成本。Windows NT 最初偏微内核，后来因性能把不少功能移回内核。
    - 浏览器 HTML5、媒体播放器等要决定脚本能否创建任务、I/O、后台运行、持久存储、联网和认证。接口越强，开发越方便，攻击面也越大。早期邮件脚本可读取联系人并自动发送邮件，导致病毒一键传播。
  - title: 3.1 进程管理（Process Management）
    source_heading: 3.1 进程管理（Process Management）
    paragraphs:
    - Shell 是用户级作业控制系统。编译多个 C 文件可表示为命令序列：分别编译源文件，再链接目标文件。把命令写入文件，Shell 就能读取并逐步创建进程。
    - UNIX C 编译器自身也可组合预处理、解析、代码生成和汇编等独立程序。这体现“小工具 + 通用组合机制”：专门程序各自做好一件事，Shell 决定流水线。
    - Chrome 可用进程隔离标签页：子进程运行同一浏览器代码但处于独立保护域，关闭感染页面可清理该进程。更强方案是每链接一个虚拟机，连客户内核被攻破也限制在
      VM。
    - UNIX 还允许进程向另一进程发即时通知，用于终止、暂停、恢复、定时器等。接收者未安装 handler 时，内核执行默认动作。这里沿用第 2 章 upcall
      机制。
  - title: 3.2 输入/输出（Input/Output）
    source_heading: 3.2 输入/输出（Input/Output）
    paragraphs:
    - 磁盘按块寻址，网络收发变长包，键盘产生字符；有些设备仅在请求后返回，有些主动产生输入。若每类设备都有专用系统调用，新设备出现就要修改内核接口和应用。
    - 句柄称文件描述符（file descriptor, fd），即使对象不是文件。Shell 启动应用时通常已打开标准输入、输出和错误描述符。
    - 原书说管道在任一端关闭或退出时终止，实践中的精确 EOF 取决于所有写端引用都关闭；fork 后忘关多余 fd 会使消费者永远等不到 EOF。
    - TCP 类似跨机器双向可靠字节流，但包含网络故障、连接建立和双向通道；不能把本地 pipe 的全部语义直接等同 TCP。
  - title: 3.3 案例：实现 Shell
    source_heading: 3.3 案例：实现 Shell
    paragraphs:
    - 内核只需提供进程和 I/O 原语，Shell 自己负责读取命令、解析语法、创建进程、设置描述符和等待任务。它不需要特权，也不需要为每种组合修改内核。
    - 告诉系统使用哪个解释器。内核/启动机制执行解释器，并把脚本文件作为参数。于是 Shell 从文件读命令，行为与从键盘读取一致。
    - 每个阶段可并行运行。Shell 等待所有阶段完成，而非每创建一个就立即等待，否则流水线会退化为串行，甚至因管道填满而死锁。
    - 局限：字节流缺少类型和模式；不同工具格式不兼容时需转换程序；错误状态不能只靠 stdout 传递，通常依靠退出码和 stderr。
  - title: 3.5 操作系统结构
    source_heading: 3.5 操作系统结构
    paragraphs:
    - Windows 的策略：同一处理器架构下，启动时动态链接主板专用 HAL；跨处理器架构通常编译不同内核二进制，近似型号有时用条件路径共享。
    - 设备种类远多于指令集，原书引用调查称 Linux 内核约 70% 代码属于设备特定软件。动态驱动让设备厂商按标准内核接口提供模块，系统启动后只加载实际设备所需代码。
    - 内核驱动拥有完整权限。普通 bug 可静默改写用户/内核数据，恶意驱动可直接植入内核病毒。原书引用研究称约 90% 系统崩溃来自驱动 bug，突出驱动隔离价值；该比例是书中时代研究，不应视为今天所有系统的恒定统计。
    - 应用调用可由库隐藏：同一 API 在宏内核中发系统调用，在微内核中编码为发给服务器的消息。内核也可把看似普通系统调用转发给服务器，因此位置对应用大体透明。
  - title: 3.6 总结与未来方向
    source_heading: 3.6 总结与未来方向
    paragraphs:
    - Shell、窗口管理器、打印服务和部分文件/网络/驱动功能可作为用户服务，利用进程隔离与 pipe。进程抽象不仅供应用，也帮助构造 OS。
    - 浏览器等应用内部有多用户、第三方代码、资源分配、任务和持久存储。传统 Windows/UNIX API 并非为这类嵌套资源管理设计，未来接口可能需要更细粒度沙箱和显式资源控制。
    - 传统内核透明决定何时调度、给多少内存、何时写盘和发包；应用仿佛独占虚拟机器。问题是应用内部也做资源管理，却不知道外层配额变化。
    - 作者类比联邦制：内核和应用像两级政府。若双方都独立分配同一资源且不沟通，会相互干扰；接口应让配额和变化可见，同时由内核保持全局隔离与公平。
  - title: 章末练习详解
    source_heading: 章末练习详解
    paragraphs:
    - 此时内核无需阻塞，只复制退出状态、回收残余 PCB/PID 信息并返回。若子仍运行，阻塞；若 PID 不是子进程或状态已被其他 wait 收集，通常立即返回错误而不是成功。
    - 不能仅以父子进程隔离作为安全沙箱：恶意作业仍继承用户权限，可能 fork bomb、联网或读取同用户文件。应结合容器/VM、只读基础镜像、无网络、最小权限和资源配额。
    - 作业控制的核心不是单个 PID，而是进程组（process group）：一条流水线中的全部进程属于同一个 PGID，终端信号一次发送给整组。
    - 竞态边界：创建作业和加入作业表期间应暂时屏蔽 SIGCHLD，否则极短任务可能在 Shell 记录 PID 前已经结束。记录完成后解除屏蔽并处理待决 SIGCHLD。
  - title: 全章总结：本章的分析方法
    source_heading: 全章总结：本章的分析方法
    paragraphs:
    - 应用需要稳定、方便的接口；功能放库、内核还是服务器，可在保持接口时调整。这样设计才能随可靠性和性能需求演化。
    - Shell 决定任务顺序和重定向，内核只保证创建、保护、权限和句柄正确。这提高创新速度，又不牺牲安全边界。
    - 打印、窗口、驱动和文件服务都可成为独立进程。微内核把这一思想推向极端，宏内核则优先直接调用性能；混合系统按调用频率和故障风险选边界。
    - 应用越来越像小型 OS。内核若完全隐藏资源变化，应用内部 GC、任务调度和缓存无法适应；未来接口需要在抽象稳定与资源可见之间重新平衡。
  - title: 复习检查清单
    source_heading: 复习检查清单
    paragraphs:
    - 能说明统一 I/O、先 open、字节流、内核缓冲和显式 close；
    - 能解释 pipe 的 EOF 条件及为什么必须关闭多余端点；
    - 能分解 IPC 延迟并理解合并调用、处理器捐赠和共享内存优化；
    - 能说明作业控制为何需要进程组、终端前台组和 SIGCHLD。
  takeaways:
  - 进程数量、引用计数、缓冲区和调用成本的推导，以及 C 接口示例，直接放在相关系统调用与案例之后；平台差异则在对应接口处说明，使抽象、实现和使用方式连续衔接。
  - UNIX 还允许进程向另一进程发即时通知，用于终止、暂停、恢复、定时器等。接收者未安装 handler 时，内核执行默认动作。这里沿用第 2 章 upcall
    机制。
  - 应用调用可由库隐藏：同一 API 在宏内核中发系统调用，在微内核中编码为发给服务器的消息。内核也可把看似普通系统调用转发给服务器，因此位置对应用大体透明。
  - 竞态边界：创建作业和加入作业表期间应暂时屏蔽 SIGCHLD，否则极短任务可能在 Shell 记录 PID 前已经结束。记录完成后解除屏蔽并处理待决 SIGCHLD。
  - 能说明作业控制为何需要进程组、终端前台组和 SIGCHLD。
check_intro: 以下问题只检验现有正文明确说明的内容。请先根据原文脉络作答，再展开参考答案核对。
checks:
- level: 概念辨析
  question: 原文如何解释「易混淆概念与常见误解」？
  answer: fd 是进程内的小整数句柄，可指文件、设备、pipe 或 socket。内核每次查表验证；同一数字在不同进程可指不同对象。
  source_heading: 易混淆概念与常见误解
- level: 边界判断
  question: 原文如何解释「3.1 进程管理（Process Management）」？
  answer: UNIX 还允许进程向另一进程发即时通知，用于终止、暂停、恢复、定时器等。接收者未安装 handler 时，内核执行默认动作。这里沿用第 2 章
    upcall 机制。
  source_heading: 3.1 进程管理（Process Management）
- level: 关系解释
  question: 原文如何解释「3.3 案例：实现 Shell」？
  answer: 内核只需提供进程和 I/O 原语，Shell 自己负责读取命令、解析语法、创建进程、设置描述符和等待任务。它不需要特权，也不需要为每种组合修改内核。
  source_heading: 3.3 案例：实现 Shell
- level: 关系解释
  question: 原文如何解释「章末练习详解」？
  answer: 此时内核无需阻塞，只复制退出状态、回收残余 PCB/PID 信息并返回。若子仍运行，阻塞；若 PID 不是子进程或状态已被其他 wait 收集，通常立即返回错误而不是成功。
  source_heading: 章末练习详解
- level: 概念复述
  question: 原文如何解释「复习检查清单」？
  answer: 能说明作业控制为何需要进程组、终端前台组和 SIGCHLD。
  source_heading: 复习检查清单
