---
uid: stanford-cs336-s26-lecture-010
type: course
document_type: lecture
course: stanford-cs336-s26
module_number: 3
lecture_number: 10
official_lecture_number: 10
display_number: 10
title: 'Lecture 10: Inference'
description: 老师如何定义 inference 的核心任务？
excerpt: 老师如何定义 inference 的核心任务？
content_lang: zh-CN
duration: '1:25'
duration_seconds: 5130
media_sources:
- provider: youtube
  label: YouTube
  url: https://www.youtube.com/watch?v=EfM546A79aM
  embed_url: https://www.youtube-nocookie.com/embed/EfM546A79aM?enablejsapi=1
  seek_template: https://www.youtube.com/watch?v=EfM546A79aM&t={seconds}s
  thumbnail: "/assets/courses/stanford-cs336-s26/thumbnails/EfM546A79aM.jpg"
thumbnail: "/assets/courses/stanford-cs336-s26/thumbnails/EfM546A79aM.jpg"
resources:
- category: lecture-code
  title: Lecture 10 official lecture_10.py
  url: "/assets/courses/stanford-cs336-s26/materials/official-materials/lectures/lecture_10.py"
  source_url: https://raw.githubusercontent.com/stanford-cs336/lectures/main/lecture_10.py
- category: lecture-rendering
  title: Lecture 10 rendered trace
  url: https://cs336.stanford.edu/lectures/?trace=lecture_10
- category: lecture-trace
  title: Lecture 10 compiled trace JSON
  url: "/assets/courses/stanford-cs336-s26/materials/official-materials/traces/lecture_10.json"
  source_url: https://raw.githubusercontent.com/stanford-cs336/lectures/main/var/traces/lecture_10.json
- category: paper
  title: 'DeepSeek-V4: Towards Highly Efficient Million-Token Context Intelligence'
  url: https://arxiv.org/pdf/2606.19348.pdf
- category: paper
  title: gqa 2023
  url: https://arxiv.org/pdf/2305.13245.pdf
- category: paper
  title: longformer 2020
  url: https://arxiv.org/pdf/2004.05150.pdf
- category: paper
  title: mistral 7b 2023
  url: https://arxiv.org/pdf/2310.06825.pdf
- category: paper
  title: mla 2024
  url: https://arxiv.org/pdf/2405.04434.pdf
- category: paper
  title: sparse transformer 2019
  url: https://arxiv.org/pdf/1904.10509.pdf
- category: reading
  title: 'Lecture 10 reading: apxml.com'
  url: https://apxml.com/posts/llm-quantization-techniques-explained
- category: paper
  title: 'Lecture 10 paper: arxiv.org'
  url: https://arxiv.org/pdf/2210.17323.pdf
- category: paper
  title: 'Lecture 10 paper: arxiv.org'
  url: https://arxiv.org/pdf/2211.17192.pdf
- category: paper
  title: 'Lecture 10 paper: arxiv.org'
  url: https://arxiv.org/pdf/2302.01318.pdf
- category: paper
  title: 'Lecture 10 paper: arxiv.org'
  url: https://arxiv.org/pdf/2306.00978.pdf
- category: paper
  title: 'Lecture 10 paper: arxiv.org'
  url: https://arxiv.org/pdf/2401.10774.pdf
- category: paper
  title: 'Lecture 10 paper: arxiv.org'
  url: https://arxiv.org/pdf/2405.12981.pdf
- category: paper
  title: 'Lecture 10 paper: arxiv.org'
  url: https://arxiv.org/pdf/2407.14679.pdf
- category: paper
  title: 'Lecture 10 paper: arxiv.org'
  url: https://arxiv.org/pdf/2303.17951.pdf
- category: paper
  title: 'Lecture 10 paper: arxiv.org'
  url: https://arxiv.org/pdf/2309.06180.pdf
- category: paper
  title: 'Lecture 10 paper: arxiv.org'
  url: https://arxiv.org/pdf/2310.18313.pdf
- category: paper
  title: 'Lecture 10 paper: arxiv.org'
  url: https://arxiv.org/pdf/2401.15077.pdf
- category: code-reference
  title: 'Lecture 10 code-reference: github.com'
  url: https://github.com/ggml-org/llama.cpp
- category: code-reference
  title: 'Lecture 10 code-reference: github.com'
  url: https://github.com/vllm-project/vllm
- category: image-reference
  title: 'Lecture 10 image-reference: images.ctfassets.net'
  url: https://images.ctfassets.net/xjan103pcp94/1LJioEsEdQQpDCxYNWirU6/82b9fbfc5b78b10c1d4508b60e72fdcf/cb_02_diagram-static-batching.png
- category: image-reference
  title: 'Lecture 10 image-reference: jax-ml.github.io'
  url: https://jax-ml.github.io/scaling-book/assets/img/cached-inference-1400.webp
- category: image-reference
  title: 'Lecture 10 image-reference: jax-ml.github.io'
  url: https://jax-ml.github.io/scaling-book/assets/img/gmqa.png
- category: image-reference
  title: 'Lecture 10 image-reference: jax-ml.github.io'
  url: https://jax-ml.github.io/scaling-book/assets/img/naive-inference-1400.webp
- category: image-reference
  title: 'Lecture 10 image-reference: jax-ml.github.io'
  url: https://jax-ml.github.io/scaling-book/assets/img/transformer-diagram.png
- category: reading
  title: 'Lecture 10 reading: jax-ml.github.io'
  url: https://jax-ml.github.io/scaling-book/inference/
- category: reading
  title: 'Lecture 10 reading: jax-ml.github.io'
  url: https://jax-ml.github.io/scaling-book/transformers/
- category: reading
  title: 'Lecture 10 reading: nvidia.github.io'
  url: https://nvidia.github.io/TensorRT-LLM/overview.html
- category: reading
  title: 'Lecture 10 reading: research.google'
  url: https://research.google/blog/looking-back-at-speculative-decoding/
- category: reading
  title: 'Lecture 10 reading: sgl-project.github.io'
  url: https://sgl-project.github.io/
- category: reading
  title: 'Lecture 10 reading: storage.googleapis.com'
  url: https://storage.googleapis.com/gweb-research2023-media/media/SpeculativeDecoding-1-Illustration.mp4
- category: reading
  title: 'Lecture 10 reading: www.baseten.co'
  url: https://www.baseten.co/blog/fp8-efficient-model-inference-with-8-bit-floating-point-numbers/
- category: image-reference
  title: 'Lecture 10 image-reference: www.datocms-assets.com'
  url: https://www.datocms-assets.com/104802/1709770809-twitter-post-20.png
- category: reading
  title: 'Lecture 10 reading: www.pymnts.com'
  url: https://www.pymnts.com/artificial-intelligence-2/2025/openai-bests-google-in-race-for-consumer-ai-token-consumption/
- category: paper
  title: 'Lecture 10 paper: www.usenix.org'
  url: https://www.usenix.org/system/files/osdi22-yu.pdf
- category: video-reference
  title: 'Lecture 10 video-reference: www.youtube.com'
  url: https://www.youtube.com/watch?v=Ob9PPLxETYU
transcript:
  jsonl: "/assets/courses/stanford-cs336-s26/lectures/010/transcript.jsonl"
  txt: "/assets/courses/stanford-cs336-s26/lectures/010/transcript.txt"
  srt: "/assets/courses/stanford-cs336-s26/lectures/010/transcript.srt"
  captions:
  - label: English captions
    url: "/assets/courses/stanford-cs336-s26/lectures/010/captions.source.srt"
  status: auto-generated
timeline: "/assets/courses/stanford-cs336-s26/lectures/010/timeline.json"
permalink: "/courses/stanford-cs336-s26/lectures/010/"
toc: true
executable_notes:
  category: lecture-code
  title: Lecture 10 official lecture_10.py
  url: "/assets/courses/stanford-cs336-s26/materials/official-materials/lectures/lecture_10.py"
  source_url: https://raw.githubusercontent.com/stanford-cs336/lectures/main/lecture_10.py
---

整理模式：仅依据当前讲次目录下 prompts\sections\001-009.md 与其中提供的 transcript、代码材料整理；未引入外部资料、未补写未给出的课件页。

## 学习目标

本讲目标是建立一套面向 inference 的完整分析框架。老师先解释为什么 inference 已经从“训练之后的附属环节”变成与产品、评测、强化学习和 agent 直接相关的核心工作负载；再用 arithmetic intensity、TTFT、latency、throughput 这些指标刻画“快”究竟是什么意思；最后系统梳理如何通过缩小 KV cache、量化、剪枝、推测采样以及动态工作负载调度来提升推理效率，同时尽量不伤害 accuracy。

## 需要的先修知识

本讲默认听众已经熟悉 Transformer 的基本构成、Q/K/V、MLP、head 和 model dimension 等术语，也默认大家能接受上一讲或前面系统课里对 arithmetic intensity、HBM、memory bandwidth、compute-bound 与 memory-bound 的分析框架。对于 GQA、RoPE、蒸馏、量化、状态空间模型等内容，老师会给出足够高层的说明，但不会从头建立全部背景。

## 老师的教学主线

整讲主线非常清楚。先从使用场景与成本结构说明 inference 为什么重要，再把“快”拆成 TTFT、latency、throughput 三种不同指标。随后老师用统一符号重新写 transformer block 和 matmul 成本，先分析 MLP，再分析 attention，最终锁定 generation attention 是 transformer inference 的根本瓶颈，因为它天然 memory-bound。接着围绕这个瓶颈展开所有优化：第一类是减少 KV cache 或参数/数值精度，如 GQA、MLA、CLA、局部注意力、量化、剪枝；第二类是不改变目标分布但改变生成流程，如 speculative sampling；第三类是面向在线服务的系统调度与内存管理，如 continuous batching、selective batching、paged attention。最后老师把所有技巧统一到一个更高层判断：真正的大机会可能来自更 inference-friendly 的新架构。

## 核心概念与依赖关系

整讲的依赖链是从工作负载差异开始的。training 能一次看到全部 token，因此可以沿序列维并行；inference 因自回归生成必须逐 token 推进。这个结构差异先导致 generation 阶段很难像 training 那样把计算单元吃满，再在定量分析中表现为 attention generation 的 arithmetic intensity 小于 1。既然瓶颈是 memory 而不是纯 FLOPs，后面的优化就自然围绕 memory 展开。KV cache 成为中心对象：它使 naive O(T^3) 生成降为可复用前缀的 cached generation，但又引入巨大的显存和内存搬运压力。于是 GQA、MLA、CLA、sliding window、DeepSeek 压缩注意力都在不同维度压缩 KV cache；quantization 与 pruning 则通过减少参数或数值精度降低 memory 压力；speculative sampling 则利用“检查比生成快”的不对称，把 compute-bound 的并行检查能力引回 memory-bound 的 sequential generation；continuous batching 与 paged attention 则把这些理论结论落到真实动态服务系统。

## 关键推导、例子与结论边界

本讲最关键的推导有三组。第一组是矩阵乘法与 MLP 的 arithmetic intensity：在 B 远小于 D、F 的近似下，简单 matmul 的强度约为 B，MLP 在 inference 中约为 B×T，因此 prefill 时若 B×T 足够大，MLP 可以 compute-bound。第二组是 attention 的强度公式 S×T/(S+T)：代入 prefill 得 S/2，代入 generation 得 S/(S+1)<1，这直接说明 generation attention 是根本瓶颈。第三组是基于 memory-bound 假设的性能统计：memory 由参数加上 B 倍 KV cache 组成，latency 近似由 memory/bandwidth 决定，throughput 近似是 B/latency，因此 batch 增大必然拉高 latency、提升 throughput，但受显存与收益递减限制。例子上，老师多次用 Llama 2 13B on H100 演示这些结论，也用 OpenAI 每天 8.6T token 对比 DeepSeek-V4 训练 32T token 来强调 inference 规模。结论边界同样被清楚说明：任何精度对比图都不能脱离模型和设定泛化；很多压缩手段是经验上有效而非数学保证；真正 lossless 的只有类似 speculative sampling 这类带严格分布修正的方法。

## 易错点与待核对项

最容易误解的地方有五个。第一，不要把“inference is memory-bound”理解成所有部分都同样 memory-bound，真正拖垮系统的是 generation attention。第二，不要把 batch 增大简单理解成“更快”，它改善 throughput 的同时也会恶化 latency，并可能直接撞上显存上限。第三，不要把 GQA、MLA、局部注意力这类方法都当作可无脑套用的免费午餐，老师反复提醒要检查 accuracy，并对论文中的经验结论保持保留态度。第四，不要把 speculative sampling 当作近似 hack，它的卖点恰恰是 exact sample。第五，不要以为 paged attention 只是工程细节，它实际上把操作系统里的分页、共享前缀、copy-on-write 思路系统化地引入了推理服务。待核对的内容主要是若干字幕疑点，例如 K/G 的口头勘误、零散 [INAUDIBLE] 段落，以及部分量化和 DeepSeek 注意力缩写的识别噪声。

## 掌握标准

学完本讲，至少应能解释：为什么 inference 的现实重要性与 agent 崛起一起上升；TTFT、latency、throughput 三个指标各自衡量什么；training 与 inference 在并行结构上为何根本不同；为什么 generation attention 的 arithmetic intensity 小于 1；KV cache 如何把 naive 生成变成可复用前缀的两阶段推理；为什么 batch 会造成 latency 与 throughput 的系统性张力；GQA、MLA、CLA、sliding window、量化、剪枝分别在压缩什么；speculative sampling 为什么既快又保持 exactness；continuous batching、selective batching、paged attention 分别解决真实在线服务中的什么问题；以及为什么老师最后认为新架构仍有巨大潜力。

## 复习顺序

建议按“先瓶颈、后优化、再系统化”的顺序复习。先看开场部分，明确 inference 与 training 的区别以及三个速度指标。然后复习 arithmetic intensity 的三步链条：普通 matmul/MLP、attention、Llama 2 13B on H100 的性能统计，把 generation attention < 1 这个结论吃透。接着按“缩 KV cache -> 降低数值/参数成本 -> 不改分布的生成技巧”复习 GQA、MLA、CLA、local attention、quantization、pruning、speculative sampling。最后再回到动态服务系统，把 continuous batching、selective batching 与 paged attention 串起来，理解它们如何把前面的理论真正落地到 live inference server。

## 逐段详细课堂笔记

<div id="section-01" class="course-note-section-anchor" aria-hidden="true">&nbsp;</div>

### 00:00:05-00:10:04

```markdown
## 本段在整讲中的作用

这一段完成整讲开场，先把 inference 定义为“给定 prompt 后尽可能准确且尽可能快地产生响应”，再解释为什么它在产品使用、评测、强化学习和 agent 工作流里都越来越重要。随后老师把“快”拆成 TTFT、latency、throughput，并提前指出 inference 与 training 的根本差别在于自回归生成不能沿序列维并行，这为后面所有关于算术强度、KV cache 和系统优化的分析建立总问题框架。涉及符号预设时，仅引用给定代码材料中的符号声明 [代码: lecture_10.py p.1]。

## 教学展开

- [00:00:05] 老师先承接上一讲 scaling laws，说明本讲暂时切换主题，专门讨论 inference。
- [00:00:18] 他把 inference 问题定义得非常直接：模型已经训练好，输入 prompt 后，要尽量准确、尽量快速地产生 response。
- [00:00:32] 接着强调 inference 虽然课程里只占一讲，但现实重要性在持续上升，因为训练完成后模型真正产生价值的阶段就是被反复使用。
- [00:00:43] 老师按使用场景展开：聊天助手、代码补全、agent、批量数据处理都依赖 inference；评测中凡是需要 generation 的任务也依赖 inference；强化学习里 rollout、打分和更新之前，同样要先做 inference。
- [00:01:33] 然后把课程总主题“效率”重新拉回本讲：训练是一笔一次性成本，而 inference 是每天反复发生的成本，因此累计计算消耗会非常大。
- [00:02:02] 老师给出量级对比：OpenAI 每天估计生成约 8.6T tokens，而 DeepSeek-V4 训练总 token 数约 32T；这意味着几天内的推理 token 量就能接近一个前沿模型训练规模。
- [00:02:41] 接着他说明 inference 在最近一年更重要的原因：过去语言模型更像 chatbot，生成内容主要供人读；现在 agent 会进行思考、推理、工具调用和自检，绝大多数生成 token 并不是给人看的，而是直接代表 compute spend。
- [00:03:42] 因而在 agent 场景里，token 数几乎没有天然上限；只要任务更复杂，就会愿意投入更多推理算力。
- [00:04:00] 老师补充生态现状：闭源 API 提供商、开源权重模型提供商都在做 inference 服务；开源社区里也有 vLLM、SGLang、TensorRT-LLM、llama.cpp 等不同定位的实现。
- [00:04:51] 在说服大家“inference 很大”之后，老师转入“快到底是什么意思”，提出三个指标：TTFT、latency、throughput。
- [00:05:14] 他逐一解释：TTFT 是用户等到第一个 token 的时间；latency 是单个请求 token 流出的速度；throughput 是多请求场景下每秒能产出的 token 数。
- [00:06:01] 老师指出 latency 与 throughput 常常一起改善，但并不总是一致，后面会看到二者之间存在张力。
- [00:07:07] 最后进入本讲最高层结论：training 时可以同时看到全部 token，所以能沿序列维并行；inference 受自回归约束只能逐 token 生成，不能这样并行，因此更难把计算单元喂满，后面会具体体现为更低的 arithmetic intensity。
- [00:08:21] 本段结尾预告全讲结构：先用数学分析 transformer inference 的 arithmetic intensity、throughput、latency，再讲缩小 KV cache、quantization、pruning、speculative decoding 和若干实际系统问题。

## 概念、符号与推导

- inference：训练完成后，给定 prompt 生成响应的阶段，其优化目标通常同时包含正确性与速度。
- TTFT：time to first token，用户从发出请求到看到第一个 token 的等待时间。
- latency：单个请求视角下的生成速度，老师用“tokens appearing for one query”来定义。
- throughput：多请求视角下的生成速度，通常以 tokens/second 衡量。
- 训练与推理的核心结构差异：
  training：能一次看到整个序列，序列维只是 tensor 的一个维度，因此可并行。
  inference：因自回归生成而必须一次产生一个 token，不能沿生成维并行。
- [代码: lecture_10.py p.1] 中本讲预先声明了后续要用的形状符号 B、S、T、D、F、N、K、H、L、V 以及常数 c、memory_bandwidth；本段只是先引出这些记号将用于后续推导。
- 本段没有正式公式推导；重点是问题定义、指标定义与工作负载差异。

## 例子与课堂提示

- OpenAI 每天 8.6T tokens 对比 DeepSeek-V4 训练 32T tokens，这个例子服务于“inference 是重复成本，量级很快超过一次训练”。
- chatbot 与 agent 的对比是本段最重要的课堂提示：chatbot 里人类阅读速度可能成为瓶颈，agent 里 token 生成量本身就是 compute 开销，所以推理加速空间更大。
- 老师列举 vLLM、SGLang、TensorRT-LLM、llama.cpp，不是做工具横评，而是提示 inference 已形成独立工程生态。
- “训练能并行看全序列，推理不能”是本讲最需要记住的总开关，后面所有 memory-bound、KV cache、continuous batching 都由这里展开。

## 本段掌握检查

1. 老师如何定义 inference 的核心任务？
答：模型训练好以后，输入 prompt，并尽量准确、尽量快速地产生 response。

2. 为什么老师说 inference 的重要性正在上升？
答：因为模型真正被使用、被评测、被拿来做 RL rollout 和 agent 行动时都依赖 inference，而且这些调用是重复发生的。

3. TTFT、latency、throughput 分别对应什么视角？
答：TTFT 对应首 token 等待时间，latency 对应单请求 token 产生速度，throughput 对应多请求总体 token 产出速度。

4. 为什么 inference 和 training 是非常不同的工作负载？
答：training 能一次看到全部 token 并沿序列维并行，inference 受自回归约束只能逐 token 生成，不能同样并行。

5. 为什么 agent 场景会让 inference 比 chatbot 场景更值得优化？
答：因为 agent 会生成大量中间 token 做思考、调用工具和自检，这些 token 直接代表 compute 开销，不受人类阅读速度限制。

## 待核对项

- [需回听 00:01:49] 字幕中“actual use app of vantage point”显然是识别错误，这里按上下文理解为从实际使用视角看 inference 是重复成本。
- [需回听 00:04:28] SGLang 被描述为“特别适合 agentic workloads，但还没那么流行”，原句细节可再核对。
```

<div id="section-02" class="course-note-section-anchor" aria-hidden="true">&nbsp;</div>

### 00:10:04-00:19:59

```markdown
## 本段在整讲中的作用

这一段从记号和矩阵乘法规则出发，把 transformer block 的张量形状、GQA 相关变量以及 arithmetic intensity 的基本回顾重新搭起来，目的是为后面正式分析 inference 的 MLP 与 attention 成本做统一语言。它承接前一段的“推理为何重要”，开始进入“怎样定量分析推理为何慢”。

## 教学展开

- [00:10:04] 老师先解释张量乘法图中的颜色含义：红色维度是 contracting dimensions，出现在两个操作数里但在结果中消失；黑色维度只出现在一个操作数里并保留在结果中；蓝色维度则是 batching dimensions，出现在两个操作数里且保留在结果中。
- [00:10:41] 有了这个约定后，老师说这套图式能最清楚地定义 transformer block，因为它把每一步张量形状和依赖关系都明确写出来。
- [00:11:34] 他快速回顾 transformer：上一层激活 X 先经过 attention，再经过 MLP。
- [00:11:51] 接着回顾 attention 的 Q、K、V 投影，并引入 N 是 query heads 数，H 是单头维度；Q 的形状是 batch × sequence × heads × head-dim。
- [00:12:13] 对 K 和 V，老师强调这里不一定仍用 N，而是用 K 表示 key/value heads 数，为后面 GQA 铺垫：K 可以比 N 小。
- [00:12:24] 他指出 attention 运算里 batch 维 B 会同时出现在两个操作数中并保留，这一点稍后会变成 inference 难点。
- [00:12:46] 之后快速回顾 MLP：包含 gate、up projection 和 down projection，不再展开实现细节，只把符号对齐。
- [00:13:11] 老师给出本讲约定：MLP 把模型维 D up-project 到 F，默认 F 视为 4D；模型维 D 会被拆到 N 个头上，所以 D = N × H。
- [00:13:38] 对 GQA，他补充 N 会再拆成 K 个组和每组的 G 个 query heads，不过现场被学生纠正了 K 和 G 的含义可能写反。
- [00:13:52] 然后区分 S 和 T：它们都与序列长度有关，但 S 表示输入/条件 token 数，T 表示输出 token 数；training 时两者相同，generation 时 T=1 而 S 是上下文长度。
- [00:14:24] 进入 arithmetic intensity 回顾后，老师把一个 B×D 与 D×F 的矩阵乘法当作 warm-up。
- [00:14:53] 他按系统课程的方式逐项数 memory I/O 和 FLOPs：读 X，读 W，做 matmul，写回结果；bf16 下读写每个元素按 2 bytes 算。
- [00:15:49] 由此得到 FLOPs 是 2BDF，bytes transferred 是 2BD + 2DF + 2BF，arithmetic intensity 等于 FLOPs 除以 bytes。
- [00:16:24] 接着做极限近似：当 B 远小于 D、F 时，可令 D 和 F 相对 B 很大，于是 intensity 约化成 B。
- [00:17:15] 老师再回忆 accelerator intensity 的比较法：用硬件峰值 FLOPs/s 除以 memory bandwidth 得到 H100 的阈值约 295；若 computation intensity 大于它则 compute-bound，否则 memory-bound。
- [00:18:04] 最后用极端例子 B=1 总结：此时 intensity 只有 1，会严重 memory-bound，而 inference 里的很多计算就接近这种“很薄的矩阵/张量”情形。
- [00:18:50] 末尾问答中，学生指出上一张 transformer 图里 K 与 G 的角色可能说反了，老师接受修正，说 K 应表示组数，G 表示每组头数。[00:20:23]

## 概念、符号与推导

- contracting dimensions：出现在两个操作数中、在结果中消失的维度。
- batching dimensions：出现在两个操作数中、在结果中保留的维度。
- transformer 约定：
  F = 4D。
  D = N × H。
  GQA 情况下 N 可拆成 K × G，但本段口头说明里 K、G 一度发生混淆，需以勘误为准。
- S 与 T：
  S 是条件/输入 token 数。
  T 是要产出 logits 的 token 数。
  training 时 S=T。
  generation 时 T=1。
- 矩阵乘法 warm-up：
  X 形状为 B×D，W 形状为 D×F。
  FLOPs = 2BDF。
  bytes transferred = 2BD + 2DF + 2BF。
  arithmetic intensity = FLOPs / bytes transferred。
- 当 B 远小于 D、F 时，intensity 近似为 B。
- accelerator intensity：硬件峰值 FLOPs/s 除以 memory bandwidth；H100 大约是 295。
- 判断标准：
  computation intensity > accelerator intensity，则 compute-bound。
  computation intensity < accelerator intensity，则 memory-bound。

## 例子与课堂提示

- 老师把 transformer 形状图称为“最 crisp 的定义”，提示学生不要只记概念名词，而要能直接看出张量依赖和形状流动。
- B=1 的矩阵向量乘例子非常关键，它服务于后面理解“为什么 inference 很容易 memory-bound”。
- 学生现场纠正 K 与 G 的定义，是课堂上很有价值的提醒：符号一旦混淆，后面关于 GQA 的成本分析就会全错。
- “matmul 的 FLOPs 是三次项，memory read/write 更像二次项”是本段的核心直觉，后面所有高算术强度结论都依赖这个对比。

## 本段掌握检查

1. 红色、黑色、蓝色维度在老师的张量图里分别表示什么？
答：红色表示 contracting 维，黑色表示只在一个操作数中出现并保留的普通维，蓝色表示 batching 维。

2. 本讲中 F、D、N、H 的基本关系是什么？
答：默认 F=4D，且 D=N×H。

3. S 与 T 在 training 和 generation 中分别如何取值？
答：training 时 S=T；generation 时 T=1，而 S 是已有上下文长度。

4. 为什么 B×D 乘 D×F 的 matmul 在 B 很小时会 memory-bound？
答：因为 FLOPs 相对于搬运参数矩阵的内存开销不够大，arithmetic intensity 近似只剩 B，B=1 时只有 1。

5. 老师如何用硬件指标判断某计算是 compute-bound 还是 memory-bound？
答：把计算本身的 arithmetic intensity 与硬件的 accelerator intensity 比较，前者大于后者则 compute-bound，否则 memory-bound。

## 待核对项

- [需回听 00:19:03-00:20:23] 学生纠正的是 K 与 G 的命名关系，字幕在这一段有多处 [INAUDIBLE]，最终以老师口头“k should be the number of groups”那句为准。
- [需回听 00:17:04] 老师把这里说成是方阵 N/3 结果的“analog”，细节表述可以再和前面系统课原推导对齐。
```

<div id="section-03" class="course-note-section-anchor" aria-hidden="true">&nbsp;</div>

### 00:19:59-00:29:54

```markdown
## 本段在整讲中的作用

这一段正式从“naive inference 怎么做”切入，提出 KV cache 作为把推理从重复计算里解救出来的第一层关键结构，并进一步计算 MLP 在 inference 场景下的 arithmetic intensity。它承接上一段的记号和 matmul 回顾，把一般算术强度分析落到推理流程本身。

## 教学展开

- [00:20:30] 老师先处理上段符号勘误，确认 K 应该表示组数，G 表示每组 query heads 数，然后回到 arithmetic intensity of inference。
- [00:20:42] 他先描述最朴素的生成流程：给定 prompt，送入 transformer，得到 logits，采样一个 token，把它拼回 prompt，再把更长的序列重新送入 transformer，如此反复。
- [00:21:19] 老师指出这个 black-box 重复调用方案虽然正确，但代价极高，因为每次生成一个 token 都要对当前长度为 T 的历史做一次 O(T^2) attention，连续生成 T 个 token 总成本会变成 O(T^3)。
- [00:22:01] 接着给出关键观察：不同前缀之间有大量重复工作。比如从 “never going to give” 继续生成和从其更长前缀继续生成，会共享此前 token 的 K、V 和激活。
- [00:22:24] 之所以能共享，是因为这里假设的是 causal transformer；往后追加 token 不会改变之前 token 的表示。如果是 bidirectional 模型，新增 token 会反向影响已有位置，就不能这样缓存。
- [00:22:42] 因此老师引出最直接的优化：把 KV cache 存在 HBM 里，在 successive token generations 之间复用。
- [00:23:10] 他用两阶段来重画推理：prefill 阶段先处理整个 prompt，填满 KV cache 并产生下一 token 的分布；之后 generation 阶段每次只输入一个新 token，同时读取已有 cache、输出下一个分布并把新增 token 对应的 KV 追加进 cache。
- [00:24:20] 老师给出 KV cache 的结构性定义：对每个 sequence、每个 token、每层、每个头，都要存一个 H 维向量。
- [00:24:38] 他再次强调 prefill 与 generation 的分工：prefill 因为一次看到整个 prompt，所以像 training 一样可并行；generation 虽仍 sequential，但至少不再重复计算历史 token 的 KV。
- [00:25:22] 有了缓存结构后，老师开始计算 MLP 层的 FLOPs 和 memory I/O，并声明只盯住 matmul，因为它们是大头，其它操作 FLOPs 少且可能融合进 matmul。
- [00:26:07] 他按顺序数：读 X；读 Wup、Wgate、Wdown；算上投影并写回；算 gate 并写回；算下投影并写回。
- [00:27:00] 由此得到 FLOPs 依赖于 B、T、D、F，bytes transferred 则由读入激活、读三组参数和三次写回共同构成。
- [00:27:15] 进一步得到 arithmetic intensity 表达式，并在假设 B×T 远小于 D、F 时把它化简为 B×T。
- [00:28:05] 老师解释这个结论的含义：prefill 时如果 batch 大、序列长，MLP 可以 compute-bound；generation 时 T=1，于是 MLP intensity 退化成 B，而 B 只是并发请求数，在服务场景里不可控、会随时间变化。
- [00:29:00] 本段末尾老师预告：continuous batching 稍后就是针对“generation 里 B 不稳定”这个问题提出的。

## 概念、符号与推导

- naive inference：每生成一个新 token，都把完整历史重新送入 transformer。
- 复杂度结论：
  单次前向在序列长度为 T 时 attention 代价是 O(T^2)。
  连续生成 T 个 token 的总代价是 O(T^3)。
- KV cache：为每个 sequence、每个历史 token、每一层、每个 KV head 存储 key/value 表示，以便后续 token 生成时复用。
- 推理分两阶段：
  prefill：处理整个 prompt，能像 training 一样并行。
  generation：每步只生成一个新 token，但复用已有 KV cache。
- MLP 层成本统计：
  FLOPs 为三次 matmul 的总和，量级是 6BTDF。
  bytes transferred 由读 X、读三组权重、写 U、写 G、写 Y 组成。
  在 B×T 远小于 D、F 时，MLP arithmetic intensity 近似为 B×T。
- generation 时 T=1，因此 MLP arithmetic intensity 近似为 B。

## 例子与课堂提示

- “never going to give” 的前缀共享例子服务于理解：KV cache 不是启发式技巧，而是 causal transformer 结构直接允许的复用。
- 老师强调“只看 matmul”是在给分析做剪枝，提示学生系统分析时先抓主导项，而不是把所有小操作一视同仁。
- generation 中 B 是并发请求数这一点非常重要，它说明服务系统的工作负载不是静态设定，而是外部流量决定的。

## 本段掌握检查

1. 为什么 naive inference 连续生成 T 个 token 会到 O(T^3)？
答：因为每次生成一个 token 都要对当前长度约为 T 的历史做一次 O(T^2) attention，重复 T 次后总量变成 O(T^3)。

2. KV cache 能成立的关键结构前提是什么？
答：模型是 causal transformer，追加新 token 不会改变旧 token 的表示，所以旧 token 的 K、V 可以复用。

3. prefill 和 generation 的差别是什么？
答：prefill 一次处理完整 prompt、可并行；generation 逐 token 生成，但每步会读取已有 cache 并追加新 token 的 KV。

4. 老师为什么在 MLP 成本分析里只看 matmul？
答：因为 matmul 是主要 FLOPs 来源，其他操作代价较小，而且常能融合进 matmul 实现里。

5. 为什么 generation 阶段的 MLP intensity 只剩 B？
答：因为 generation 时 T=1，原本近似为 B×T 的 intensity 就退化成 B，而 B 只是并发请求数。

## 待核对项

- [需回听 00:20:44] 字幕中 “here is a prompt never going to give you” 口语跳转较乱，这里按老师常用的 Rick Astley 前缀示例理解。
- [需回听 00:25:49] 字幕把 matmul 误识为 “MAmmoTH”，这里按语义统一整理为 matmul。
```

<div id="section-04" class="course-note-section-anchor" aria-hidden="true">&nbsp;</div>

### 00:29:54-00:39:53

```markdown
## 本段在整讲中的作用

这一段把 attention 层的 arithmetic intensity 算出来，并由此给出整讲最核心的结论之一：prefill 基本可算 compute-bound，而 generation 中的 attention 天生 memory-bound，且这是 transformer inference 的根本瓶颈。它直接承接上一段的 MLP 分析，完成“为什么 inference 慢”的定量闭环。

## 教学展开

- [00:29:54] 老师先给出 attention 层的总体结果：FLOPs 是 B×S×T×D 量级，bytes transferred 则由读 Q/K/V 和写输出构成。
- [00:30:08] 他提醒大家，matmul 一般都会让 FLOPs 的多项式次数比 bytes transferred 高一阶，关键是看前面的系数长什么样。
- [00:30:22] 对 attention，这个系数会化成 S×T / (S+T)。
- [00:30:30] 老师先代入 prefill，即 T=S，此时 intensity 变成 S/2。只要序列够长，attention prefill 仍然能保持较高算术强度，因此总体还算不错。
- [00:30:55] 他特别提醒，这里没有 batch 维 B 的帮助，稍后会解释原因。
- [00:31:02] 再看 generation，令 T=1，则 intensity 变成 S/(S+1)，严格小于 1，几乎可以把它视作 1，而 1 对 H100 这类硬件来说远远达不到 compute saturation 所需的水平。
- [00:31:25] 老师因此说这才是真正的 bottleneck：此前把 MLP、attention、prefill、generation 都分开算，就是为了找到这个位置。
- [00:31:46] 接着他解释为什么 attention 与 MLP 不同。MLP 层里所有 sequence 共享同一组 MLP weights，所以 batch 变大时，相当于把一次读入的参数复用在更多样本上，算术强度上升。
- [00:32:18] attention 层则不同，每个 sequence 都有自己的 KV cache，Q/K/V 都依赖 B，所以增大 batch 只是并列地多做几份独立工作，不能像共享参数那样摊薄内存代价。
- [00:33:17] 老师把这种现象类比回最开始的 “按坐标批量做点积” 的差算术强度例子：attention generation 本质上更接近一堆 dot product，而不是能很好复用权重的大 matmul。
- [00:33:46] 他再次指向 transformer 图中那个蓝色的 B：正是这个 batching dimension 同时存在于 Q、K、V 中，导致 attention 的算术强度不会随 B 增长。
- [00:33:58] 最后汇总四个子结论：prefill MLP intensity 是 B×S，prefill attention 是 S/2，generation MLP 是 B，而 generation attention 小于 1；其中最后一项是根本瓶颈。
- [00:34:58] 老师用一句“以后再听到别人说 inference is memory bound，你现在知道为什么了”作为这段结论。
- [00:35:08] 随后他开始把这些直觉转成 throughput、latency 与 TTFT 的框架，指出一旦知道瓶颈是 memory-bound，推理时间就可以优先从 memory transfer 的角度理解。
- [00:36:04] 为了具体化，他拿 Llama 2 13B on H100 做例子，列出 sequence length、model dim、feedforward dim、query/KV heads、head dim、layers、vocab size 和带宽等配置。
- [00:37:16] 接着他定义性能统计量：参数量、总内存占用、latency、throughput；内存包含参数与 KV cache，总 latency 由 memory I/O 主导，throughput 则是在并行生成 B 个 token 的前提下由 latency 反推。
- [00:39:34] 片段结尾老师给出第一个 sanity check：该配置下参数量约 13B，线性于 B 的那一项来自 KV cache。

## 概念、符号与推导

- attention arithmetic intensity：
  intensity = S×T / (S+T)。
- prefill：
  T=S，因此 intensity = S/2。
- generation：
  T=1，因此 intensity = S/(S+1) < 1。
- 关键区别：
  MLP 的参数矩阵不依赖 batch，可被多条 sequence 共享，所以 batch 变大能提升 intensity。
  attention 的 Q/K/V 或 KV cache 依赖 batch，每个 sequence 都带着自己的历史，因此 batch 增大不能提高同一份内存读入的复用率。
- 汇总结论：
  prefill MLP intensity = B×S。
  prefill attention intensity = S/2。
  generation MLP intensity = B。
  generation attention intensity < 1。
- Llama 2 13B 示例中，后续 latency 和 throughput 计算都建立在“memory I/O 主导”的假设上。

## 例子与课堂提示

- 老师把 generation attention 比作一堆 dot product，这个类比服务于理解“为什么它的算术强度很差”。
- “别人说 inference is memory bound”那句总结是课堂提示：不是所有推理子模块都一样慢，而是 attention generation 这块把整体拖成 memory-bound。
- Llama 2 13B on H100 的例子提醒学生：这些公式不是纯抽象推导，后面会直接代进真实硬件和模型配置求数值。

## 本段掌握检查

1. attention 层的 arithmetic intensity 在一般情形下是什么？
答：S×T / (S+T)。

2. 为什么 prefill attention 还算可接受，而 generation attention 很糟？
答：prefill 时 T=S，intensity 是 S/2，会随序列变长而升高；generation 时 T=1，intensity 变成 S/(S+1)，始终小于 1。

3. 为什么增大 batch 能帮到 MLP，却帮不到 attention generation？
答：因为 MLP 权重在 batch 内共享，而 attention 的 KV cache 是每条 sequence 自己持有的，batch 变大只是增加独立副本。

4. 整个 transformer inference 的根本瓶颈是哪一项？
答：generation 阶段的 attention，因为它的 arithmetic intensity 小于 1，天然 memory-bound。

5. 老师为什么可以先从 memory transfer 而不是 FLOPs 去理解 latency？
答：因为在 inference 尤其是 generation 里，瓶颈已经是 memory-bound，所以时间首先由搬运多少内存决定。

## 待核对项

- [需回听 00:31:07] 字幕把 generation 说成 “generated nation intensity”，这里按语义整理为 generation intensity。
- [需回听 00:36:17] 字幕中的 “Llama 2.13B” 应为 “Llama 2 13B”。
```

<div id="section-05" class="course-note-section-anchor" aria-hidden="true">&nbsp;</div>

### 00:39:53-00:49:53

```markdown
## 本段在整讲中的作用

这一段把上一段的符号公式实例化成 batch size 与性能的具体权衡，再顺势进入第一类推理加速思路：缩小 KV cache，其中首先讨论 GQA。它起到从“解释瓶颈”切换到“开始动手优化”的作用。

## 教学展开

- [00:39:53] 老师先把 Llama 2 13B 的性能统计解释完：总 memory 对 B 呈线性函数，线性项来自 KV cache，常数项来自参数；latency 由 memory/bandwidth 决定，因此也随 B 线性增长；throughput 则像 B/(B+常数) 这样随 B 增长但最终趋于饱和。
- [00:41:40] 他先看 batch size 1：大约得到 0.008 秒每 token 的 latency，以及 124 tokens/s 的 throughput。
- [00:42:02] 增大 batch size 后，latency 会变差，但 throughput 会变好，于是“快”出现两个相反方向：低延迟和高吞吐未必同时实现。
- [00:42:37] 若继续盲目增大 batch，虽然 throughput 继续上升，但会先撞上显存上限，因为 KV cache 占用随 B 扩张，最终 H100 放不下。
- [00:43:31] 老师用“等公交车”的比喻说明：大 batch 对单个请求来说意味着更久等待，所以 latency 像等车；但对系统总运力来说，一车拉很多人，所以 throughput 像公交总吞吐量。
- [00:44:17] 因而得到明确 trade-off：小 batch 更利于 latency，大 batch 更利于 throughput。
- [00:44:31] 老师只简略提到并行化的另一个维度：可以把模型复制 M 份，保持 latency 不变而把 throughput 乘上 M；更复杂的 model/KV shard 留给相关阅读。
- [00:45:01] 接着把 TTFT 放进来：TTFT 基本等于 prefill 时间，因此如果想优化 TTFT，prefill 阶段应倾向小 batch；generation 阶段若追求吞吐则倾向大 batch。
- [00:45:49] 在有了 throughput、latency、TTFT 的框架后，老师开始讲“怎样更快”。他先说各种技术横跨架构与系统两端，但最显然的方向是：memory 是瓶颈，而 KV cache 占了很多 memory，所以先设法把 KV cache 变小。
- [00:46:56] 第一种方法是 GQA。老师回顾：多头注意力里每个 token 都有对应 query/key/value；GQA 保持 query 头数不变，但只保留较少的 key/value groups。
- [00:47:29] 他用 K 的含义说明端点：MHA 是 K=N，不缩减；MQA 是 K=1，但效果通常太差；GQA 在两者之间寻找精度与速度平衡。
- [00:48:00] 随后引用 GQA 论文结果：随着 K 变小，time per sample 会下降；即便不像 MQA 那样极端，适中的 K 也能带来明显速度收益。
- [00:48:40] 老师解释原因：GQA 把 KV cache 缩小了 N/K 倍，而 inference 是 memory-bound，所以减少内存直接转成 latency 和 throughput 改善。
- [00:49:00] 再回到 Llama 例子，原始配置下 K=N、B=64；若改成 1:5 的 GQA 稀疏比，memory 会明显下降，从而 latency 改善、throughput 上升。
- [00:49:50] 本段末尾老师提醒：latency 与 throughput 并不总是彼此冲突，如果一种改动直接减少 memory，两者可以同时改善；真正造成 tension 的主要是 batch 维度。

## 概念、符号与推导

- 性能统计的结构：
  memory = 参数占用 + B×每序列 KV cache。
  latency ≈ memory / memory_bandwidth。
  throughput ≈ B / latency。
- 随 batch 变化的趋势：
  latency 随 B 近似线性上升。
  throughput 随 B 上升但会逐渐趋于饱和，并在显存限制前就出现收益递减。
- TTFT：本讲里可近似看作 prefill 时间。
- GQA：
  MHA：K=N。
  MQA：K=1。
  GQA：1 < K < N。
- GQA 的直接数学收益：KV cache 缩小 N/K 倍，因此 memory I/O 同步减小。

## 例子与课堂提示

- batch=1 给出 0.008 秒/token 与 124 tokens/s，是老师把符号模型落地为数值直觉的第一步。
- “等公交车”比喻非常有帮助：它清楚地区分了单请求 latency 和系统 throughput 不是同一个目标函数。
- “latency 和 throughput 不总冲突，batch 才是 tension 的来源”是本段最关键的课堂提示，说明如果减少的是 memory 本身，两者可以一起变好。

## 本段掌握检查

1. 为什么 batch 增大时 latency 变差？
答：因为 KV cache 随 B 增长，单次生成要读写更多缓存，而且单个请求要等整个 batch 一起推进。

2. 为什么 batch 增大时 throughput 往往变好？
答：因为参数读入成本能被更多并发 sequence 摊薄，系统一次能生成更多 token。

3. TTFT 在老师这里近似对应什么？
答：对应 prefill 时间。

4. GQA 相比 MHA 改动了什么？
答：保持 query 头数不变，但把 key/value heads 数减少为更小的组数 K。

5. 为什么 GQA 可能同时改善 latency 和 throughput？
答：因为它直接缩小 KV cache，减少 memory I/O，而 inference 的瓶颈正是 memory。

## 待核对项

- [需回听 00:46:20] 老师说“beat it into your head that memory is the bottleneck”是口语化表达，这里只保留实质含义。
- [需回听 00:49:32] 字幕中的 “a kind of a sparsity of 1 one five” 应是指 K:N = 1:5 的 GQA 比例。
```

<div id="section-06" class="course-note-section-anchor" aria-hidden="true">&nbsp;</div>

### 00:49:53-00:59:48

```markdown
## 本段在整讲中的作用

这一段继续沿着“缩小 KV cache”推进，把 GQA 的 batch-accuracy 权衡讲完，再快速铺开 MLA、CLA 和 local/sliding window attention 三条路线。它承接上一段已经建立的“memory 是推理瓶颈”判断，目标是说明只要能减少 KV cache，就可能同步改善 latency 与 throughput，但所有这类技巧都必须接受精度检验。

## 教学展开

- [00:49:53] 老师先总结上一页：减少 memory 往往能同时改善 latency 和 throughput，真正制造二者张力的是 batch 维度本身。
- [00:50:06] 他接着做联合调参示例：原先 batch=256 会因为 KV cache 太大而放不进显存；做了 GQA 之后，同样的大 batch 可以装下，于是虽然 latency 因 batch 变大而变差，但 throughput 会继续按系统总吞吐的方向上升。
- [00:50:53] 不过老师马上补上限制条件：凡是 lossy 改动都必须回到 accuracy 检查。GQA 论文显示时间更好、评测也还不错，但这些结果不能脱离具体模型去泛化。
- [00:51:33] 随后进入 DeepSeek 的 multi-latent attention，仍然围绕“减少 KV cache”这条主线。
- [00:51:46] 老师先对比 MHA、GQA 和 MLA：MHA 为每个 token 保留完整 K/V；GQA 减少 K/V 头数；MLA 则先把激活压缩成更低维 latent，再从 latent 重建 K 和 V，因此缓存里只需存较小的压缩表示。
- [00:52:43] 他给出 DeepSeek-V2 的量级示例：把约 16000 维压到 512 维，压缩幅度非常大，所以缓存缩减也非常明显。
- [00:53:14] 老师同时指出 MLA 的一个 wrinkle：它与 RoPE 不完全兼容，因为 RoPE 直接作用在 K/V 上，所以要额外保留一部分维度处理 RoPE，但总体上仍然大幅减小了缓存。
- [00:53:54] 接着他看经验结果：GQA 和 MHA、MLA 之间的精度关系并非单向确定，DeepSeek 的结果甚至与先前 GQA 论文存在张力，所以还是要按具体模型具体看。
- [00:54:51] 当学生问“为什么不直接缩小模型维度”时，老师回答这会 indiscriminately 地伤害整体能力；真正的技巧在于找到可以被压缩而损失较小的局部结构，这通常需要实验摸索。
- [00:55:31] 下一种方法是 cross-layer attention：不再让每一层都单独维护一套 KV，而是让部分层复用前一层或少数层的缓存，相当于把“跨头共享”推广为“跨层共享”。
- [00:56:15] 老师给出的经验结论是：CLA 能改进 accuracy 与 KV cache 大小的 Pareto frontier。
- [00:56:54] 然后进入 local/sliding window attention：每次生成只看最近 K 个 token，于是 KV cache 不再随总序列长度增长，对长上下文尤其有吸引力。
- [00:57:51] 不过老师提醒，显式窗口只有 K 并不等于模型只能看到 K 个 token，因为多层堆叠仍允许信息逐层向更远处传播，所以有效上下文会大于单层窗口。
- [00:58:28] 代价是 expressivity 下降，accuracy 可能受损，因此常见补救是 local 与 global/full attention 交错成 hybrid 结构。
- [00:59:12] 本段结尾进入学生提问：若在 hybrid 模型里比较 sliding window 与 linear attention，它们的 trade-off 应该怎么理解。老师在下一段继续回答。[00:59:20-00:59:48]

## 概念、符号与推导

- GQA：通过减少 KV heads 数 K，按 N/K 比例缩小 KV cache。
- MLA：
  先把激活压到更低维 latent c。
  再从 c 投影回 K、V。
  只缓存 c，因此缓存大小由 N×H 级别降到 C 级别。
- CLA：跨层共享 KV cache，而不是每层保留独立副本。
- sliding window attention：只缓存最近 K 个 token，使缓存大小不再直接随总序列长度增长。
- hybrid attention：把 local/sliding window 层与 global/full attention 层交错，以缓解局部窗口带来的表达损失。
- 本段没有新的正式公式推导，重点是几类 KV cache 压缩路线及其精度-速度权衡。

## 例子与课堂提示

- “做了 GQA 后 batch=256 能装进显存”这个例子服务于说明：缩小 KV cache 不只直接提速，还会释放更大的 batch 调节空间。
- DeepSeek-V2 从约 16000 压到 512 维，是 MLA 压缩力度最直观的例子。
- “不要 indiscriminately 缩小模型维度”是本段的重要课堂提示：推理优化要找局部瓶颈，而不是平均削弱整个模型。
- local attention 的“有效上下文比单层窗口更大”提醒学生不要把显式窗口大小与整网可传播范围简单画等号。

## 本段掌握检查

1. 为什么做了 GQA 之后，系统有时可以进一步提高 batch size？
答：因为 GQA 缩小了 KV cache，占用更少显存，所以原本放不下的大 batch 可能就能装下。

2. MLA 与 GQA 在压缩 KV cache 的方式上有什么根本区别？
答：GQA 是减少 KV heads 数量；MLA 是先把产生 K/V 的表示压到低维 latent，再从 latent 重建 K/V，只缓存低维表示。

3. 为什么老师不赞成直接把模型维度整体缩小来代替这些方法？
答：因为那会无差别地伤害模型能力，而这些结构化方法试图只压缩成本高、但相对可压缩的部分。

4. sliding window attention 的主要优势是什么？
答：KV cache 不再随总序列长度线性增长，因此对长上下文推理特别有利。

5. 为什么 local/sliding window attention 往往要与 global attention 混用？
答：因为纯局部窗口会损失表达能力和精度，混入部分全局层可以缓解这种损失。

## 待核对项

- [需回听 00:50:58] 老师对 GQA 评测结果的保留态度是口语化表达，这里只保留“不能脱离具体模型泛化”的实质结论。
- [需回听 00:59:20-00:59:48] 这一段主要是学生提问开场，真正的回答放在下一段，因此这里只记问题背景，不抢答后续内容。
```

<div id="section-07" class="course-note-section-anchor" aria-hidden="true">&nbsp;</div>

### 00:59:48-01:09:47

```markdown
## 本段在整讲中的作用

这一段回答上段留下的“linear attention versus sliding window”问题，并把“压缩 KV cache”这条主线进一步扩展到线性/状态空间方法、DeepSeek-V4 的更激进压缩注意力、quantization 与 pruning。它承接上一段的结构化缓存缩减方法，逐步过渡到更一般的“降低 inference complexity 但尽量不伤 accuracy”的工程套路。

## 教学展开

- [00:59:48] 老师先回答上一段的问题：最朴素的 linear attention 会把整段历史的 K/V 压成一个单向量，因此缓存大小与序列长度无关；更复杂的 gated net、delta net、Mamba 试图在压缩历史的同时少忘掉重要信息。
- [01:00:15] 他指出，这些线性/状态空间方法也常与 sliding window attention 互相替代或混合使用，因为它们各自抓住的信息类型不同。
- [01:00:24] 老师给出直观区分：若任务依赖局部高分辨率细节，sliding window 往往更好；若只需要过去的粗略摘要，线性注意力可能更合适。
- [01:00:46] 当学生追问“长上下文是不是线性注意力一定更优”时，老师强调没有 free lunch。若任务像 needle in a haystack，需要远距离精确取回某个细节，把全部历史压成小状态很可能直接丢失信息。
- [01:01:17] 因此他观察到，实践里很多人会用 hybrid architectures：仍保留一部分较长或更精细的注意力，同时引入更便宜的压缩机制。
- [01:01:41] 对 sliding window 与 Mamba/delta net 的进一步比较，老师给出倾向性判断：后者通常更强，因为 sliding window 一旦截断就没有额外表达空间，而递归压缩模型至少有机会学到更有效的状态总结。
- [01:02:18] 随后老师快速点名 DeepSeek 继续在注意力机制上做创新：在 MLA 之外，又提出 compressed sparse attention、DeepSeek sparse attention、heavily compressed attention 等更复杂的压缩路线。
- [01:02:50] 他用图解释基本结构：先把每 M 个 token 压成一个 token，再用更轻量的小注意力打分来选择要保留的子集，之后再进一步压缩，从而极力减少真正参与后续计算和缓存的内容。
- [01:03:44] 到这里老师做本阶段总结：只要 inference 是 memory-bound，减少 KV cache 就会直接改善 throughput 和 latency；可从更低维缓存、跨层共享、local attention、linear attention、甚至 diffusion 这种非自回归生成方式去找机会。
- [01:04:30] 然后话题转到 quantization。老师把它定位成更偏 systems 的手段：核心思想就是减少数值精度，memory 变小后，memory-bound inference 自然会更快。
- [01:05:05] 他列出常见精度选项，从 bf16 往下到 fp8、int8、int4，精度越低越省内存，但也越可能伤害准确性。
- [01:05:16] 接着区分 QAT 与 PTQ：QAT 在训练过程中显式模拟量化误差，因此通常更稳；但代价是需要昂贵的大规模训练。更常见的是训练完再量化的 PTQ。
- [01:06:00] 老师说明简单 PTQ 会为每层或每个 tensor 单独决定 scale 与 zero point，但效果未必最好；更聪明的方法如 GPTQ 会利用 Hessian 信息逐层量化，并把误差补偿回未量化权重中。
- [01:06:38] 再往前一步是 activation-aware quantization：既然某些激活通道更大、更重要，就让打到这些通道的权重保留更高精度，而把大部分其他位置压到极低精度，从而兼顾速度与精度。
- [01:07:39] 最后进入 model pruning：粗暴地删掉不重要的层、头、隐藏单元，然后再通过后训练或蒸馏把模型“修补”回来。
- [01:08:05] 老师给出 NVIDIA 方案的高层步骤：先在小 calibration set 上估计各部分重要性，再移除不重要组件，最后针对关心的数据或任务继续训练以 heal 模型。
- [01:09:00] 经验结果显示，15B 模型可以被裁到接近 8B，精度只轻微受损，而额外修补训练成本远低于从头再训一遍。
- [01:09:17] 本段结尾老师再次归纳：游戏规则就是尽量降低 inference complexity，通常表现为减少参数或 KV cache，同时别把 accuracy 伤得太多。

## 概念、符号与推导

- naive linear attention：把整段历史的 K/V 压成一个较小的总结状态，因此缓存大小与序列长度脱钩。
- sliding window attention 与 linear attention 的直觉分工：
  sliding window 更保留局部高分辨率细节。
  linear/state-space 更擅长维护压缩后的长期摘要。
- DeepSeek 更激进压缩路线：
  先做 token 压缩。
  再做轻量打分筛选。
  再进一步压缩保留的表示。
- quantization：通过降低数值精度减小 memory I/O。
- QAT：训练时模拟量化误差。
- PTQ：训练后再量化。
- GPTQ：使用 Hessian 信息逐层量化并补偿误差。
- AWQ：依据激活重要性保留极少数高精度权重通道。
- pruning：识别不重要层/头/隐藏单元，删除后再通过额外训练或蒸馏修复。

## 例子与课堂提示

- “needle in a haystack” 例子说明：长上下文任务并不自动偏向强压缩方法，因为过度压缩可能丢失正需要被检索的远距离细节。
- 老师对 sliding window 和 Mamba/delta net 的比较给出一个重要工程直觉：前者是硬截断，后者至少还有学习更优压缩策略的空间。
- AWQ 的课堂提示很明确：不是所有权重都值得同样精度，少量关键通道往往决定量化后精度是否崩掉。
- pruning 的“先砍再治”思路提醒学生，这类方法并不优雅，但在工程上可能极其有效。

## 本段掌握检查

1. 为什么老师说 linear attention 对长上下文没有 free lunch？
答：因为如果任务要求从长历史中精确找回某个细节，把历史压成小状态可能直接丢失那条关键信息。

2. sliding window attention 和 linear/state-space 方法各更擅长什么？
答：sliding window 更擅长保留局部高分辨率细节；linear/state-space 更擅长维护压缩后的长期摘要。

3. QAT 和 PTQ 的主要区别是什么？
答：QAT 在训练中模拟量化误差，效果通常更稳但成本高；PTQ 在训练完成后量化，成本低且更常用。

4. GPTQ 比朴素 PTQ 更聪明在哪里？
答：它利用 Hessian 信息逐层量化，并把量化误差补偿回尚未量化的权重中。

5. pruning 为什么通常需要后续修补训练？
答：因为直接删除层、头或隐藏单元会破坏原模型能力，需要再训练或蒸馏来恢复性能。

## 待核对项

- [需回听 01:02:18-01:03:32] DeepSeek 若干注意力缩写在字幕里较乱，这里仅保留老师明确说出的“压缩、筛选、再压缩”机制，不擅自补充缩写全称。
- [需回听 01:06:55] 字幕中的 “in three” 应为 int3。
```

<div id="section-08" class="course-note-section-anchor" aria-hidden="true">&nbsp;</div>

### 01:09:47-01:19:44

```markdown
## 本段在整讲中的作用

这一段把 speculative sampling 的数学保证、实践 sweet spot 和动态批处理问题系统讲完，并且与代码材料中的 lecture outline、性能公式、GQA/量化/剪枝/推测采样安排互相对齐。它承担从“单个技术点”过渡到“整讲后半段完整路线图”的作用。[代码: lecture_10.py p.2] 已提前把这几部分列为 Taking shortcuts、speculative sampling 与 dynamic workloads 三组主题；本段正对应这些部分。[代码: lecture_10.py p.7][代码: lecture_10.py p.8][代码: lecture_10.py p.10][代码: lecture_10.py p.11][代码: lecture_10.py p.12][代码: lecture_10.py p.13][代码: lecture_10.py p.14][代码: lecture_10.py p.15]

## 教学展开

- [01:09:47] 老师先把上一段的“更快模型 + distillation 修补”说完整，这和课程代码总览里 Distillation recipe 的三步结构一致：定义更快架构、用原模型初始化、再修补。[代码: lecture_10.py p.2]
- [01:09:54] 针对“怎样区分重要与不重要层/通道”的提问，老师解释可以用 calibration set 让样本通过模型，观察激活大小；接近 0 的死单元更可能被移除，幅值更大的通道更应保留。这与 AWQ 利用激活信息分配精度的思路呼应。[代码: lecture_10.py p.12]
- [01:10:40] 当学生追问“若所有激活都很高怎么办”时，老师说这些方法之所以能奏效，依赖一个经验事实：不同通道的重要性确实会拉开；否则相关技术本身也站不住。
- [01:11:21] 对“某神经元总是 100 是否就有意义”的追问，老师指出可进一步看 variance；若只是高均值低方差，未必该保留原单元，可能还有别的吸收方式。
- [01:11:44] 接着正式转入 speculative sampling，并明确把它定义为 lossless 方法；此前压缩 KV cache、量化、剪枝基本都可能伤精度，这里要看一种不改目标分布的加速。
- [01:12:02] 他重新回顾两阶段：prefill 能并行编码并给概率，compute-bound；generation 逐 token，memory-bound。所以“checking is faster than generation”成为该方法的出发点，这与代码材料里 speculative_sampling 的开场完全一致。[代码: lecture_10.py p.14]
- [01:12:31] 具体做法是让更便宜的 draft model p 先猜几个 token，再让目标模型 q 并行审核。老师强调搭配的平衡性：p 小而便宜，q 大而准，但只拿来做并行检查。[代码: lecture_10.py p.14]
- [01:13:17] 视频演示展示了普通大模型逐 token 生成与 speculative decoding 的 burst generation 对比，帮助理解为什么该方法能在 wall-clock 时间上更快。
- [01:13:53] 然后老师讲算法：先从 p 采样 K 个 token，再用 q 并行算这些 token 的 logits。
- [01:14:31] 接受判据是按 min(1, q/p) 接受；若不接受，就转向 residual distribution。老师把它说成 rejection sampling 的修改版，主要区别是这里总能至少产出一个候选，这一点也与代码材料中“always generate at least one candidate”一致。[代码: lecture_10.py p.14]
- [01:15:15] 最核心的理论性质是 exactness：尽管用了近似草稿模型，最终样本仍精确来自目标模型。代码材料还给出两词表元素 A/B 的例子证明这个性质。[代码: lecture_10.py p.14]
- [01:15:41] 实践上老师指出 sweet spot：draft token 太少，target 侧批并行没被充分利用；太多，拒绝率会上升。论文示例里大约 3-4 个 token 最合适。[代码: lecture_10.py p.14]
- [01:16:04] 他还指出 draft model 应尽量接近 target model，因此前面介绍的压缩、蒸馏等技巧也都可以反过来服务 speculative decoding 的 draft model 构造。
- [01:16:56] 之后转入 dynamic workloads：真实在线推理中，请求到达时间不同、有共享前缀、长度也不同，这正是课程代码总览里列出的三个 live traffic 难点。[代码: lecture_10.py p.2]
- [01:17:31] Orca 的 continuous batching 用 iteration-level scheduling 解决“不能等一个完整 batch 才开始生成”的问题：每个 decode step 给当前 batch 的每条序列生成一个 token，完成的踢出，新来的立即加入。[代码: lecture_10.py p.15]
- [01:18:35] 这会带来 ragged array 问题，于是老师引入 selective batching：attention 因依赖各自长度，很难真正合并；但非 attention 计算可以把不同序列拼成一个 mega sequence 统一过 MLP，从而保留大量 FLOPs 级别的批处理收益。[代码: lecture_10.py p.15]
- [01:19:35] 本段最后把话题自然引到 paged attention，这也是代码总览里 dynamic workloads 的另一半。[代码: lecture_10.py p.2]

## 概念、符号与推导

- speculative sampling 中的模型记号：
  p：draft model。
  q：target model。
- 生成流程：
  1. 从 p 采样 K 个 draft tokens。
  2. 用 q 并行计算这些候选的概率。
  3. 以 min(1, q/p) 的规则接受；否则从 residual distribution 采样。
- exact sample：虽然中间用了提议分布 p，但最终生成分布严格等于 q。[代码: lecture_10.py p.14]
- sweet spot 原因：
  K 太小，target 端并行未被充分利用。
  K 太大，draft 与 target 的偏差积累导致拒绝率升高。
- continuous batching：按 decode iteration 动态维护 batch。
- selective batching：
  attention 保持按各自序列长度处理。
  非 attention 计算可沿 token 维拼接处理。[代码: lecture_10.py p.15]

## 例子与课堂提示

- 代码里的 Distillation recipe 与老师口头“先造更快模型，再 repair”完全对齐，提示学生把压缩、蒸馏、部署看成同一条工程链条。[代码: lecture_10.py p.2]
- speculative sampling 的 A/B 两元素证明例子是课堂提示：它不是“通常差不多对”，而是有严格概率保证的 exact 方法。[代码: lecture_10.py p.14]
- 老师把 continuous batching 描述成动态更新中的 batch，而不是静态成块的 batch，这有助于理解为什么训练时常见的整齐张量假设在在线推理里会失效。
- selective batching 最容易误解的地方是“为什么不是所有算子都能拼起来做”，老师特意用 attention 与 MLP 的差别提醒学生注意算子结构。

## 本段掌握检查

1. speculative sampling 为什么被老师归为 lossless？
答：因为尽管用 draft model 提议候选，但最终接受规则和 residual sampling 保证输出分布仍精确等于 target model。

2. draft token 数为什么有一个甜点区而不是越多越好？
答：太少无法充分利用 target 端并行，太多会提高拒绝率，降低整体收益。

3. 为什么前面讨论的压缩与蒸馏技术对 speculative decoding 仍然有价值？
答：因为它们可以用来构造更便宜、但尽量接近 target 的 draft model。

4. continuous batching 解决了什么核心问题？
答：解决在线请求到达时间不同、不能等静态 batch 凑齐再开始生成的问题。

5. selective batching 为什么主要把 attention 和非 attention 分开处理？
答：因为 attention 强依赖各自序列长度，难以合并；非 attention 部分可以把不同序列拼接后统一处理。

## 待核对项

- [需回听 01:14:35] 老师口语里的接受规则描述略跳跃，这里以代码材料中的 rejection-sampling 版本解释为准。[代码: lecture_10.py p.14]
- [需回听 01:17:35] Orca 被老师称为“very early on”的系统，课堂里没有进一步展开时间背景，这里不额外补历史信息。
```

<div id="section-09" class="course-note-section-anchor" aria-hidden="true">&nbsp;</div>

### 01:19:44-01:25:24

```markdown
## 本段在整讲中的作用

这一段把 paged attention 讲完，并给出全讲总结：inference 为何重要、为何 memory-bound、有哪些优化方向、以及为什么新架构可能从根上改善推理友好性。它是整讲收束段，把前面分散的方法统一回“减少 KV cache、减少 memory 压力、但尽量不伤精度”的主线，同时把操作系统分页思想作为服务系统侧的代表性收尾。[代码: lecture_10.py p.16]

## 教学展开

- [01:19:44] 老师先承接上一段，说 continuous batching 只是当时 vLLM 体系里一个核心想法，接着回答“KV cache 到底怎么存”。
- [01:19:55] 他指出在线请求进入后必须把 KV cache 放到显存某处，而这会遇到 fragmentation。
- [01:20:16] 老师区分两种碎片化：
  internal fragmentation：因为不知道请求何时结束，若按最大长度一次性分配整段缓存，就会浪费大量未用空间。
  external fragmentation：不同请求之间会留下难以再次利用的小空隙。
- [01:20:53] 于是系统工程师把操作系统里已经解决过的分页思想搬到推理里：不要给一条序列分配一整块连续 KV cache，而是把它切成固定大小的非连续 blocks。[代码: lecture_10.py p.16]
- [01:21:11] 老师用 “four score and seven years ago our fathers brought forth” 的前缀举例，说序列会被按 block size 4 这样的粒度切开，block 之间可散落在不同物理位置，只要索引表能追踪映射即可。
- [01:21:58] 有了 block 化表示后，不同请求还能共享前缀 cache。若很多请求共用同一 system prompt，就可以只算一次并复用其 KV cache。[代码: lecture_10.py p.16]
- [01:22:24] 另一个场景是同一 prompt 要采样多个回答，前缀同样可以共享；只有响应部分才需要各自独立扩展。[代码: lecture_10.py p.16]
- [01:22:43] 老师进一步解释 copy-on-write：若两条采样路径暂时走出相同 token，就继续共享同一个 prefix block；一旦后续 token 不同，就在 block 级别分叉。这正对应代码材料里的“share prefixes, copy-on-write at the block level”。[代码: lecture_10.py p.16]
- [01:23:28] 他略提还有许多没时间展开的优化，例如把 block read 与 attention 融合的 kernel、使用更新的 FlashAttention/FlashDecoding、以及 CUDA graphs 来减少 kernel launch overhead。[代码: lecture_10.py p.16]
- [01:23:46] 最后进入全讲总结：inference 非常重要；虽然模型还是同一个 transformer，但推理任务要求它做的是与训练非常不同的工作，因此整体表现为很强的 memory-bound，且在线场景下工作负载高度动态。
- [01:24:07] 老师回顾可用技术：quantization、新架构、prune/distill、speculative sampling，以及系统侧的 paging 与 speculative execution。
- [01:24:19] 他再次提炼原则：这些方法虽然形式不同，但都围绕同一主线，即减少 KV cache 或更一般地降低 inference complexity，同时尽量不伤 accuracy。
- [01:24:40] 收束时老师特别强调一个没展开透的话题：state-space models、linear attention、diffusion 等新架构可能有巨大潜力，因为 transformer attention 加 KV cache 的构造从根上就不是 inference-friendly。
- [01:25:06] 因此，如果能设计出一开始就面向 inference 的新架构，可能会带来比现有补丁式优化更大的飞跃。
- [01:25:18] 本讲以“下次 Tatsu 回来讲 scaling laws part 2”结束。

## 概念、符号与推导

- fragmentation：
  internal fragmentation：为最坏长度预留但实际未使用的缓存空间浪费。
  external fragmentation：请求之间留下难以再利用的小块空隙。
- PagedAttention：把单条序列的 KV cache 切成固定大小 blocks，这些 block 在物理内存中可不连续，只通过索引映射为逻辑序列。[代码: lecture_10.py p.16]
- prefix sharing：多个请求共享同一 system prompt 或同一前缀时，共用对应 blocks 的 KV cache。
- copy-on-write：共享路径一旦产生不同 token，再从分叉点开始复制新 block，而不是预先复制整个前缀。[代码: lecture_10.py p.16]
- 系统侧附加优化：融合 block read 与 attention、使用更高效 attention kernels、利用 CUDA graphs 减少 launch overhead。[代码: lecture_10.py p.16]
- 整讲总原则：降低 inference complexity，尤其减少 KV cache/memory 压力，同时尽量保持 accuracy。

## 例子与课堂提示

- 旧时代硬盘碎片整理的比喻很形象，帮助把 KV cache 内存管理问题直接映射到熟悉的操作系统概念。
- “four score and seven years ago ...” 的例子服务于解释 blocks 只要求逻辑连续、不要求物理连续。
- system prompt 共享和一 prompt 多 sample 共享，是 paged attention 真正有业务价值的两个典型场景。
- 老师最后对新架构的评论是整讲的重要提醒：现有很多优化都是在给 transformer inference 打补丁，真正大的增益可能来自更根本的架构替换。

## 本段掌握检查

1. internal fragmentation 和 external fragmentation 的区别是什么？
答：internal fragmentation 是为最大长度预留但没用完的空间浪费；external fragmentation 是不同请求之间留下的零碎空隙难以复用。

2. PagedAttention 的核心思想是什么？
答：把一条序列的 KV cache 切成固定大小的非连续 blocks，通过索引映射来组织，而不要求整段连续分配。

3. 为什么 paged attention 适合 system prompt 共享？
答：因为共享前缀对应的 blocks 可以直接复用，不必为每个请求重复计算并存储同一段 KV cache。

4. copy-on-write 在这里具体意味着什么？
答：多条生成路径在前缀相同时继续共享同一组 blocks，直到某一步 token 不同才从分叉点复制新 block。

5. 老师为什么说新架构可能比现有技巧更有潜力？
答：因为 transformer 的 attention 与 KV cache 机制从根上就不够 inference-friendly，若架构从设计之初就面向推理，可能获得更大的系统性收益。

## 待核对项

- [需回听 01:19:44] 开头“but this is the core idea at that time”承接上一页口头转场，具体所指对象在本段字幕里不完整，这里不扩写。
- [需回听 01:24:34] 老师把 paging 与 speculative execution 并列为系统思路，后者在本讲中主要借 speculative sampling 侧面体现，未单独做传统系统定义。
```
