# 整理任务

- 课程：Stanford CS336 Language Modeling from Scratch (Spring 2026)
- 本讲：Lecture 07: Parallelism
- 片段：01:09:53-01:19:51
- YouTube 章节：未提供
- 材料模式：结合 transcript 与课件

请按下面结构输出 Markdown：

## 本段在整讲中的作用
说明老师此处试图解决什么问题，以及它与前文的连接。无法从片段判断前文时明确说明。

## 教学展开
严格按老师实际讲解顺序记录。保留“为什么提出 -> 如何定义 -> 怎样推导/解释 -> 如何举例 -> 得出什么结论”的链条。

## 概念、符号与推导
逐项记录定义、符号、公式、前提和推导步骤。没有公式时写“本段无公式推导”，不要自行补充。

## 例子与课堂提示
记录老师的例子、类比、演示、反例、经验判断和易错点，并说明它们服务于哪个知识点。

## 本段掌握检查
列出 3-5 个可用 transcript 和课件回答的问题，最后给出简短参考答案。

## 待核对项
列出字幕疑点、课件与口述差异或缺失上下文；没有则写“无”。

# 课件材料

--- [课件: lecture_07.py p.16] ---
标题：def pipeline_parallelism_main (lines 492-539)
正文：
def pipeline_parallelism_main(rank: int, world_size: int, data: tensor, num_layers: int, num_micro_batches: int):
    setup(rank, world_size)  # @stepover

    # Use all the data
    data = data.to(cuda_if_available(rank))
    batch_size = data.size(0)  # @inspect batch_size
    num_dim = data.size(1)  # @inspect num_dim

    # Split up layers
    local_num_layers = int_divide(num_layers, world_size)  # @inspect local_num_layers @stepover

    # Each rank gets a subset of layers
    local_params = [get_init_params(num_dim, num_dim, rank) for layer in range(local_num_layers)]  # @stepover

    # Forward pass

    # Break up into micro batches to minimize the bubble
    micro_batch_size = int_divide(batch_size, num_micro_batches)  # @inspect micro_batch_size @stepover
    if rank == 0:
        # The data
        micro_batches = data.chunk(chunks=num_micro_batches, dim=0)
    else:
        # Allocate memory for activations
        micro_batches = [torch.empty(micro_batch_size, num_dim, device=cuda_if_available(rank)) for _ in range(num_micro_batches)]

    for x in micro_batches:
        # Get activations from previous rank
        if rank - 1 >= 0:
            dist.recv(tensor=x, src=rank - 1)

        # Compute layers assigned to this rank
        for param in local_params:
            x = x @ param
            x = F.gelu(x)

        # Send to the next rank
        if rank + 1 < world_size:
            print(f"[pipeline_parallelism] Rank {rank}: sending {summarize_tensor(x)} to rank {rank + 1}", flush=True)  # @stepover
            dist.send(tensor=x, dst=rank + 1)

    text("Not handled: overlapping communication/computation to eliminate pipeline bubbles")

    # Backward pass: homework exercise

    cleanup()  # @stepover

############################################################
讲者备注：
Python source lines 492-539

--- [课件: lecture_07.py p.17] ---
标题：def setup (lines 540-551)
正文：
def setup(rank: int, world_size: int):
    """Initializes the distributed environment (called at start of process)."""
    # Specify where master lives (rank 0), used to coordinate (actual data goes through NCCL)
    os.environ["MASTER_ADDR"] = "localhost"
    os.environ["MASTER_PORT"] = "15623"

    if torch.cuda.is_available():
        dist.init_process_group("nccl", rank=rank, world_size=world_size)
    else:
        dist.init_process_group("gloo", rank=rank, world_size=world_size)
讲者备注：
Python source lines 540-551

--- [课件: lecture_07.py p.18] ---
标题：def cleanup (lines 552-556)
正文：
def cleanup():
    """Cleans up the distributed environment (called at end of process)."""
    torch.distributed.destroy_process_group()
讲者备注：
Python source lines 552-556

--- [课件: lecture_07.py p.19] ---
标题：class DisableDistributed (lines 557-576)
正文：
class DisableDistributed:
    """
    Context manager that temporarily disables distributed functions (replaces with no-ops).
    This is for when we're tracing the lecture, since we can't trace through
    multiprocessing, so we just want to run the function directly without
    distributed communication.
    """
    def __enter__(self):
        self.old_functions = {}
        for name in dir(dist):
            value = getattr(dist, name, None)
            if isfunction(value):
                self.old_functions[name] = value
                setattr(dist, name, lambda *args, **kwargs: None)

    def __exit__(self, exc_type, exc_value, traceback):
        for name in self.old_functions:
            setattr(dist, name, self.old_functions[name])
讲者备注：
Python source lines 557-576

--- [课件: lecture_07.py p.20] ---
标题：def spawn (lines 577-593)
正文：
def spawn(func: Callable, world_size: int, *args, **kwargs):
    """
    Launches `world_size` processes that each calls `func` on world_size, args, kwargs.
    Note: if we are being traced (inside edtrace), we just run the function directly without multiprocessing and disable distributed functions.
    """
    # Note: assume kwargs are in the same order as what main needs
    if not sys.gettrace():
        # This is the normal code path for multiprocessing
        args = (world_size,) + args + tuple(kwargs.values())
        mp.spawn(func, args=args, nprocs=world_size, join=True)
    else:
        # If we're being traced (inside edtrace), just run the function directly.
        with DisableDistributed():  # @stepover
            args = (0, world_size,) + args + tuple(kwargs.values())
            func(*args)
讲者备注：
Python source lines 577-593

--- [课件: lecture_07.py p.21] ---
标题：def get_init_params (lines 594-599)
正文：
def get_init_params(num_inputs: int, num_outputs: int, rank: int) -> nn.Parameter:
    """Create parameters and put them on the `rank`-th GPU."""
    torch.random.manual_seed(0)  # For reproducibility
    return nn.Parameter(torch.randn(num_inputs, num_outputs, device=cuda_if_available(rank)) / math.sqrt(num_outputs))
讲者备注：
Python source lines 594-599

--- [课件: lecture_07.py p.22] ---
标题：def int_divide (lines 600-605)
正文：
def int_divide(a: int, b: int):
    """Return a / b and throw an error if there's a remainder."""
    assert a % b == 0
    return a // b
讲者备注：
Python source lines 600-605

--- [课件: lecture_07.py p.23] ---
标题：def summarize_tensor (lines 606-609)
正文：
def summarize_tensor(tensor: tensor) -> str:
    return "x".join(map(str, tensor.shape)) + "[" + str(round(tensor.view(-1)[0].item(), 4)) + "...]"
讲者备注：
Python source lines 606-609

--- [课件: lecture_07.py p.24] ---
标题：def render_duration (lines 610-619)
正文：
def render_duration(duration: float) -> str:
    if duration < 1e-3:
        return f"{duration * 1e6:.2f}us"
    if duration < 1:
        return f"{duration * 1e3:.2f}ms"
    return f"{duration:.2f}s"


if __name__ == "__main__":
    main()
讲者备注：
Python source lines 610-619

# Transcript

[01:09:53] Within each layer, it's going to get all the dimensions,
[01:09:56] and it's also going to get all the--
[01:09:59] well, one of the ranks is going to get all the data.
[01:10:04] Every rank is going to see all the data in some form.
[01:10:09] So this is the way it's going to work.
[01:10:14] So we have all the data, which is, again,
[01:10:20] the batch size times num dim.
[01:10:23] And I'm going to split up the layers.
[01:10:26] So local num layers is going to be the number of layers
[01:10:29] that a particular rank is going to handle.
[01:10:33] And so now I'm going to have local params, which
[01:10:38] is basically only-- there's local number of layers
[01:10:41] number of them.
[01:10:42] But within each layer, I'm going to do the num dim by num dim.
[01:10:48] So one thing, maybe [INAUDIBLE] will talk more
[01:10:54] about this on Wednesday, is this idea of micro-batches.
[01:11:00] So maybe I'll just present this and explain
[01:11:03] why I'm doing things this way.
[01:11:05] So in addition to splitting up the layers,
[01:11:08] I'm also going to split up the batch
[01:11:10] into a bunch of micro-batches.
[01:11:14] So if I'm rank 0, then I get the data,
[01:11:21] and I'm going to chunk it up into a number of micro-batches.
[01:11:25] And for each micro-batch, what I'm going to do
[01:11:32] is receive it from the previous rank,
[01:11:39] and then do the feed forward pass only on the layers that
[01:11:44] are assigned to this rank.
[01:11:45] And then I'm going to send to the next rank.
[01:11:52] So here I'm actually using these receive and send,
[01:11:55] which are pointwise operations.
[01:11:57] So I didn't cover those before, but they're fairly explanatory.
[01:12:02] This basically says I'm rank and I'm going
[01:12:06] to send this tensor over to--
[01:12:09] sorry, I'm going to receive this tensor from rank minus 1.
[01:12:14] And this says I'm going to send tensor x to rank plus 1.
[01:12:29] So basically, the reason why I'm talking about micro-batches is--
[01:12:36] and [? Tatsu ?] will talk more about this on Wednesday--
[01:12:39] is that in pipeline parallelism, so you have
[01:12:42] one rank that gets the data.
[01:12:46] It processes some of the layers, and then
[01:12:49] it sends it to the next GPU, and it process some of the layers.
[01:12:54] And it then sends it to the next GPU.
[01:12:57] So this is a very natural way of dividing a deep network.
[01:13:01] But the problem is that you get these
[01:13:03] what are called pipeline bubbles, where while you're not
[01:13:09] processing, you're kind of waiting around for other tensors
[01:13:13] to process.
[01:13:15] And this ends up being quite inefficient.
[01:13:18] So the idea behind micro-batches is that you break it up
[01:13:23] into smaller batches, so you can process it quickly,
[01:13:25] send it on to the next one.
[01:13:27] So this can reduce the number of pipeline bubbles.
[01:13:32] So the other thing I will mention
[01:13:34] that is not handled in this very kind of naive version
[01:13:38] is the idea of overlapping communication and computation,
[01:13:41] which is actually very important to pipeline parallelism.
[01:13:45] This basically gives you the right structure.
[01:13:47] If you put an I before these, then it becomes kind of async.
[01:13:54] You have to add more things to manage the code.
[01:13:59] And the idea here is that you want to be--
[01:14:03] while you're computing here, you can be
[01:14:08] receiving data or sending data.
[01:14:11] So computation and communication should overlap.
[01:14:14] So that reduces the amount of time you actually spend waiting.
[01:14:23] So a few things that are kind of missing here, which we'll
[01:14:30] hopefully fill in next time.
[01:14:32] So the communication versus computation overlap,
[01:14:36] which is especially crucial in pipeline parallelism.
[01:14:42] I didn't mention in data parallelism,
[01:14:44] this also happens, because I just did a forward pass
[01:14:49] and then at the end, I'm just doing all these all reduces.
[01:14:52] But if you're clever, then on the backward pass,
[01:14:55] as soon as the gradients are done,
[01:14:57] you can start sending that.
[01:14:59] And that's something that will be
[01:15:00] explored in the assignment two.
[01:15:03] And this, again, allows you to just overlap communication
[01:15:07] and computation more.
[01:15:11] We had some questions about what about general models.
[01:15:14] Again, I think this MLP gives you
[01:15:19] essentially most of what you need
[01:15:24] for understanding the basics.
[01:15:28] Some of the larger models just require a lot more bookkeeping,
[01:15:31] so it's harder to see the core algorithms.
[01:15:37] There are other types of parallelism
[01:15:38] that we haven't covered.
[01:15:40] So sequence parallelism takes a whole sequence
[01:15:43] and chops it up into pieces.
[01:15:45] And that allows you to parallelize the attention
[01:15:47] computation, expert parallelism, which allows you to parallelize
[01:15:53] the experts for MOEs.
[01:15:54] And this is where the all to all that I mentioned comes in.
[01:15:58] And then also different combinations
[01:16:02] of different parallelization techniques, which also
[01:16:06] will show up in the assignment.
[01:16:10] So one thing to note is that which
[01:16:14] parallelism technique you choose is
[01:16:18] going to be strongly dependent on the hardware.
[01:16:22] For example, tensor parallelism.
[01:16:24] There's a lot of communication, because for every layer,
[01:16:27] you need to send all these activations, which
[01:16:30] are fairly big.
[01:16:32] So generally, tensor parallelism happens within a node on NVLink
[01:16:36] or where you have high bandwidth.
[01:16:41] Whereas you wouldn't do tensor parallelism
[01:16:43] past an NVLink domain.
[01:16:47] Whereas a pipeline parallelism, you'll see people using it.
[01:16:51] And generally, this can tolerate much slower interconnects.
[01:16:58] So some of the decentralized training work
[01:17:02] uses pipeline parallel, because your nodes are--
[01:17:06] GPUs are halfway across the world.
[01:17:09] But you wouldn't want to do tensor parallel in that setting.
[01:17:14] So sometimes when you look at these combinations,
[01:17:18] it will be tensor parallel within a node
[01:17:21] and then pipe data parallel or FSDP
[01:17:29] and then pipeline parallelism if you need it.
[01:17:35] There's other effects, such as if you do data parallel,
[01:17:39] you might be able to do data parallel quite a bit.
[01:17:43] But then you start hitting to something
[01:17:45] called the critical batch size, where
[01:17:47] if you start increasing the batch size too much,
[01:17:50] it doesn't actually help you, in which case
[01:17:52] you're kind of wasting your compute
[01:17:54] and then you're better off using tensor parallel.
[01:17:56] So there's a bunch of these considerations,
[01:17:58] which we'll talk more about as we go through the class.
[01:18:03] So final note.
[01:18:05] So TPUs came up a little bit.
[01:18:08] One thing to note is that on purpose, we are using PyTorch
[01:18:15] and not just using PyTorch, but really using
[01:18:17] the collective operations in a very primitive way,
[01:18:22] so you can see mechanically what's happening.
[01:18:26] Another approach, especially if you're in [INAUDIBLE] TPU land,
[01:18:30] is that you can simply define the model and the starting
[01:18:35] strategy.
[01:18:36] And the compiler actually handles a lot
[01:18:38] of the decision of how to--
[01:18:44] basically, what kind of communication operations
[01:18:47] you need.
[01:18:48] You basically say, well, this piece of data
[01:18:51] needs to be here and here and here.
[01:18:54] And then the compiler does some magic to figure out what.
[01:18:59] So that's appealing.
[01:19:00] But obviously, it would take a lot of the joy
[01:19:04] out of actually building things from scratch.
[01:19:09] Just to summarize, so there's many ways to parallelize.
[01:19:15] You can cut by data, cut by tensor or expert,
[01:19:20] cut by pipeline or sequence.
[01:19:24] We looked at data parallelism.
[01:19:26] We only did DDP.
[01:19:28] Next time we'll do FSDP and 0.
[01:19:32] Tensor parallelism, as I mentioned,
[01:19:34] requires very fast interconnects.
[01:19:37] Pipeline less so.
[01:19:39] But you need to really work hard to reduce these pipeline
[01:19:42] bubbles.
[01:19:44] And then maybe at a high level, we
[01:19:47] see this pattern come up a lot.
