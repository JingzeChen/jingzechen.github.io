# 整理任务

- 课程：Stanford CS336 Language Modeling from Scratch (Spring 2026)
- 本讲：Lecture 07: Parallelism
- 片段：00:39:58-00:49:56
- YouTube 章节：未提供
- 材料模式：结合 transcript 与课件

请按下面结构输出 Markdown：

## 本段在整讲中的作用
说明老师此处试图解决什么问题，以及它与前文的连接。无法从片段判断前文时明确说明。

## 教学展开
严格按老师实际讲解顺序记录。保留“为什么提出 -> 如何定义 -> 怎样推导/解释 -> 如何举例 -> 得出什么结论”的链条。

## 概念、符号与推导
逐项记录定义、符号、公式、前提和推导步骤。没有公式时写“本段无公式推导”，不要自行补充。

## 例子与课堂提示
记录老师的例子、类比、演示、反例、经验判断和易错点，并说明它们服务于哪个知识点。

## 本段掌握检查
列出 3-5 个可用 transcript 和课件回答的问题，最后给出简短参考答案。

## 待核对项
列出字幕疑点、课件与口述差异或缺失上下文；没有则写“无”。

# 课件材料

--- [课件: lecture_07.py p.6] ---
标题：def collective_operations_main (lines 249-286)
正文：
def collective_operations_main(rank: int, world_size: int):  # @inspect rank world_size
    """This function is running asynchronously for each process (rank = 0, ..., world_size - 1)."""
    setup(rank, world_size)

    ### All-reduce (dist = torch.distributed)
    dist.barrier()  # Waits for all processes to get to this point (in this case, for print statements)

    data = tensor([0., 1, 2, 3], device=cuda_if_available(rank)) + rank  # Both input and output

    print(f"Rank {rank} [before all-reduce]: {data}", flush=True)
    dist.all_reduce(tensor=data, op=dist.ReduceOp.SUM, async_op=False)  # Modifies tensor in place
    print(f"Rank {rank} [after all-reduce]: {data}", flush=True)

    ### Reduce-scatter
    dist.barrier()

    input = torch.arange(world_size, dtype=torch.float32, device=cuda_if_available(rank)) + rank  # Input
    output = torch.empty(1, device=cuda_if_available(rank))  # Allocate output

    print(f"Rank {rank} [before reduce-scatter]: input = {input}, output = {output}", flush=True)
    dist.reduce_scatter_tensor(output=output, input=input, op=dist.ReduceOp.SUM, async_op=False)
    print(f"Rank {rank} [after reduce-scatter]: input = {input}, output = {output}", flush=True)

    ### All-gather
    dist.barrier()

    input = output  # Input is the output of reduce-scatter
    output = torch.empty(world_size, device=cuda_if_available(rank))  # Allocate output

    print(f"Rank {rank} [before all-gather]: input = {input}, output = {output}", flush=True)
    dist.all_gather_into_tensor(output_tensor=output, input_tensor=input, async_op=False)
    print(f"Rank {rank} [after all-gather]: input = {input}, output = {output}", flush=True)

    text("Indeed, all-reduce = reduce-scatter + all-gather!")

    cleanup()
讲者备注：
Python source lines 249-286

--- [课件: lecture_07.py p.7] ---
标题：def benchmarking (lines 287-300)
正文：
def benchmarking():
    text("How fast does communication happen?")

    # All-reduce
    spawn(all_reduce, world_size=4, num_elements=100 * 1024**2)

    # Reduce-scatter
    spawn(reduce_scatter, world_size=4, num_elements=100 * 1024**2)

    text("References:")
    link(title="How to reason about collective operations", url="https://github.com/NVIDIA/nccl-tests/blob/master/doc/PERFORMANCE.md#allreduce")
    link(title="Sample benchmarking code", url="https://github.com/stas00/ml-engineering/blob/master/network/benchmarks/all_reduce_bench.py")
讲者备注：
Python source lines 287-300

--- [课件: lecture_07.py p.8] ---
标题：def all_reduce (lines 301-337)
正文：
def all_reduce(rank: int, world_size: int, num_elements: int):
    setup(rank, world_size)  # @stepover

    # Create tensor
    data = torch.randn(num_elements, device=cuda_if_available(rank))

    # Warmup
    dist.all_reduce(tensor=data, op=dist.ReduceOp.SUM, async_op=False)
    torch.cuda.synchronize()  # Wait for CUDA kernels to finish
    dist.barrier()            # Wait for all the processes to get here

    # Perform all-reduce
    start_time = time.time()
    dist.all_reduce(tensor=data, op=dist.ReduceOp.SUM, async_op=False)
    torch.cuda.synchronize()  # Wait for CUDA kernels to finish
    dist.barrier()            # Wait for all the processes to get here
    end_time = time.time()

    duration = end_time - start_time
    print(f"[all_reduce] Rank {rank}: all_reduce(world_size={world_size}, num_elements={num_elements}) took {render_duration(duration)}", flush=True)  # @stepover

    # Measure the effective bandwidth
    dist.barrier()
    size_bytes = data.element_size() * data.numel()
    sent_bytes = size_bytes * 2 * (world_size - 1)  # 2x because send + receive, world_size-1 steps in all-reduce
    total_duration = world_size * duration
    bandwidth = sent_bytes / total_duration
    print(f"[all_reduce] Rank {rank}: all_reduce measured bandwidth = {round(bandwidth / 1024**3)} GB/s", flush=True)

    # Notes:
    # - Effective bandwidth ~ 2 * size_bytes / total_duration
    # - Independent of world_size
    # - Independent of topology (ring or tree)

    cleanup()  # @stepover
讲者备注：
Python source lines 301-337

# Transcript

[00:39:58] unnecessarily.
[00:40:02] So let's try an all reduce.
[00:40:05] So I'm going to create this tensor, 0, 1, 2, 3.
[00:40:11] And I have my rank.
[00:40:14] Just to make it more interesting,
[00:40:15] each rank is going to have a different tensor.
[00:40:21] I'm going to print out what I have before the all reduce.
[00:40:24] So now I'm going to skip over here and see
[00:40:27] what gets printed out.
[00:40:30] So rank 0 before all reduce has 0, 1, 2, 3.
[00:40:36] Rank 1 has 1, 2, 3, 4, and so on.
[00:40:38] It's the same example as I showed before.
[00:40:41] Notice that the print statements are coming in whatever order
[00:40:44] the hardware feels like it, because it's running async.
[00:40:47] But all the data is there.
[00:40:51] So now if I do all reduce, and this you pass in--
[00:40:57] this is a PyTorch function.
[00:40:59] It passes in this tensor.
[00:41:02] You pass in the reduction operation, which is a sum.
[00:41:05] And I'm going to say don't do async.
[00:41:08] And what this does is it calls, in this case, it would be gloo,
[00:41:13] but it could be NCCL, in which case, it
[00:41:16] would spin up the Cuda kernels.
[00:41:18] It would do the communication.
[00:41:19] It takes care of everything for you.
[00:41:22] And then it basically writes in place to the data.
[00:41:26] So after the all reduce, I have the all reduce operation,
[00:41:31] remember, which is the sum of each of these columns
[00:41:34] but replicated across all the ranks.
[00:41:41] So that's all reduce.
[00:41:45] And if you wanted to be fancier and do async,
[00:41:49] then you could say async equals true.
[00:41:51] But then it would screw up all these print statements.
[00:41:53] So I'm trying to put more barriers than I would normally
[00:41:57] would.
[00:41:59] Yeah, question?
[00:42:01] [INAUDIBLE]
[00:42:09] So the question is the rank the GPU.
[00:42:12] For this class, the rank is the GPU.
[00:42:19] So let's try another example here.
[00:42:23] So I'm going to do a reduce scatter.
[00:42:26] So here, I'm going to create an input, which
[00:42:31] is 0 through the world size.
[00:42:36] And I'm going to have the output.
[00:42:44] I'm going to allocate the output.
[00:42:46] And so what does this look like before I do the reduce scatter?
[00:42:50] It looks like, let's see, this, where it's the same input
[00:42:59] and the output happens to be zeros,
[00:43:01] but it could be just anything.
[00:43:05] And then I do the reduce scatter tensor.
[00:43:09] Here instead of writing in place,
[00:43:10] I have an output tensor and an input tensor.
[00:43:13] I say I want to do the sum and then
[00:43:17] afterwards, I get basically the input is not touched,
[00:43:22] but the output I get the reduction
[00:43:26] of each component written into the respective ranks.
[00:43:37] So yeah, question?
[00:43:41] [INAUDIBLE]
[00:43:51] So the question is, how does it work
[00:43:53] to do the all reduces asynchronous?
[00:43:56] So what this means is that this is like a monolithic operation.
[00:44:01] You say go do the all reduce.
[00:44:03] And it's spinning up Cuda kernels.
[00:44:06] It's going to do the communication.
[00:44:09] And remember, Cuda is already kind of async with respect
[00:44:14] to the processes, and now we have
[00:44:16] all the processes being async.
[00:44:18] And the point is that this code just would return.
[00:44:25] And then you could do other things.
[00:44:29] So a typical thing, which I'm not
[00:44:32] going to talk about this class, is overlapping computation
[00:44:36] and communication.
[00:44:38] So for example, you can do this operation,
[00:44:43] and then you can go ahead and load some other data
[00:44:46] for the next step, which is independent of this operation.
[00:44:50] And then when you want to make sure that you actually are done,
[00:44:56] then you can call a weight or a barrier.
[00:45:04] So let's do the final one, which is all gather.
[00:45:08] So by now, I think you get the idea.
[00:45:11] Here I'm going to set as the input
[00:45:13] the output of the reduce scatter.
[00:45:17] I'm going to allocate an output.
[00:45:20] So before the all gather, it looks like this.
[00:45:24] I have my results from the reduce scatter here.
[00:45:29] The output is just allocated.
[00:45:32] It happens to have some values in it, but don't worry about it.
[00:45:38] And then after I do the all gather into tensor,
[00:45:45] and then I will have all the different inputs gathered
[00:45:55] onto all the different ranks.
[00:45:59] And you can see here that, indeed,
[00:46:03] proof via example that all reduce
[00:46:05] is equal to reduce scatter plus all gather.
[00:46:13] And then just to wrap things up, just as I started by setup, then
[00:46:21] I clean up, which it's good practice to clean up.
[00:46:30] So that was your first example of a Torch distributed program.
[00:46:38] So let's do some benchmarking.
[00:46:44] This will be quick, since I want to actually move on to part two.
[00:46:50] So how fast does communication happen?
[00:46:53] So let's do an all reduce.
[00:46:56] So here, I'm going to all reduce with 100 million elements.
[00:47:05] Oops.
[00:47:10] Sorry, I messed this up.
[00:47:15] So all reduce.
[00:47:17] So I'm going to create this tensor
[00:47:21] with this number of elements.
[00:47:24] And remember, just like before when we do benchmarking,
[00:47:26] we warm up first.
[00:47:29] And here, I'm going to call the Cuda synchronize and also
[00:47:33] the barrier, just to make sure that because there's
[00:47:37] two forms of asynchrony here, the Cuda kernels
[00:47:39] and the different processes.
[00:47:40] And just want to make sure everything
[00:47:42] is kind of not running and is done before I start the time.
[00:47:49] And I'm going to do the all reduce.
[00:47:53] And then wait again with the synchronize in the barrier
[00:48:00] and stop the time.
[00:48:02] So remember, this is running for every single rank.
[00:48:09] So if I look at the output, here for rank 0, 2, 1, 3,
[00:48:14] I have a different time potentially,
[00:48:16] because they're all different processes.
[00:48:19] Each of them is going to report a certain measurement.
[00:48:23] And if you want to report one number,
[00:48:25] you can take the average, for example.
[00:48:30] So now one thing that is useful to do,
[00:48:35] which is analogous to when we were computing MFU,
[00:48:38] is to measure the effective bandwidth.
[00:48:42] And the idea here is that, well, this took 1.6 milliseconds.
[00:48:48] How much-- is that good or bad?
[00:48:53] So to compute the effective bandwidth, what
[00:48:56] we're going to do is to compute essentially
[00:48:58] how many bytes were sent and should be
[00:49:02] sent during this computation.
[00:49:06] And then if you divide by the total time,
[00:49:08] then you get the effective bandwidth.
[00:49:10] So the size of what I'm sending around
[00:49:15] is the size of each element times the number of elements.
[00:49:19] So that's basically the number of bytes of this data tensor.
[00:49:25] How many bytes get actually sent?
[00:49:27] So this needs some unpacking.
[00:49:30] So for all reduce, if you think about,
[00:49:35] let's just say for simplicity, you do a rank 0 plus rank 1
[00:49:39] plus rank 2 plus rank 3.
[00:49:42] You need to iterate this world size minus 1 steps,
[00:49:45] because there's a world size minus 1 addition operations.
[00:49:50] So that's this factor.
[00:49:52] There's a 2, because you need to both send and reduce.
