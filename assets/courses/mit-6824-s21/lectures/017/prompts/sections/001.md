# 整理任务

- 课程：MIT 6.824 Distributed Systems (Spring 2021)
- 本讲：Lecture 16 - Big Data - Spark
- 片段：00:00:00-00:09:53
- YouTube 章节：未提供
- 材料模式：结合 transcript 与课件

请按下面结构输出 Markdown：

## 本段在整讲中的作用
说明老师此处试图解决什么问题，以及它与前文的连接。无法从片段判断前文时明确说明。

## 教学展开
严格按老师实际讲解顺序记录。保留“为什么提出 -> 如何定义 -> 怎样推导/解释 -> 如何举例 -> 得出什么结论”的链条。

## 概念、符号与推导
逐项记录定义、符号、公式、前提和推导步骤。没有公式时写“本段无公式推导”，不要自行补充。

## 例子与课堂提示
记录老师的例子、类比、演示、反例、经验判断和易错点，并说明它们服务于哪个知识点。

## 本段掌握检查
列出 3-5 个可用 transcript 和课件回答的问题，最后给出简短参考答案。

## 待核对项
列出字幕疑点、课件与口述差异或缺失上下文；没有则写“无”。

# 课件材料

--- [课件: l-spark.txt p.1] ---
标题：6.824 2021 Lecture 16: Spark
正文：
6.824 2021 Lecture 16: Spark

Resilient Distributed Datasets: A Fault-Tolerant Abstraction for
In-Memory Cluster Computing Zaharia et al., NSDI 2012

why are we looking at Spark?
  widely-used for datacenter computations
  generalizes MapReduce into dataflow
  supports iterative applications better than MapReduce
  successful research: ACM doctoral thesis award

three main topics:
  programming model
  execution strategy
  fault tolerance

let's look at page-rank
  here's SparkPageRank.scala from the Spark source repository
  like the code in Section 3.2.2, with more detail

     1      val lines = spark.read.textFile("in").rdd
     2      val links1 = lines.map{ s =>
     3        val parts = s.split("\\s+")
     4        (parts(0), parts(1))
     5      }
     6      val links2 = links1.distinct()
     7      val links3 = links2.groupByKey()
     8      val links4 = links3.cache()
     9      var ranks = links4.mapValues(v => 1.0)
    10  
    11      for (i <- 1 to 10) {
    12        val jj = links4.join(ranks)
    13        val contribs = jj.values.flatMap{
    14          case (urls, rank) =>
    15            urls.map(url => (url, rank / urls.size))
    16        }
    17        ranks = contribs.reduceByKey(_ + _).mapValues(0.15 + 0.85 * _)
    18      }
    19  
    20      val output = ranks.collect()
    21      output.foreach(tup => println(s"${tup._1} has rank:  ${tup._2} ."))

page-rank input has one line per link, extracted from a big web crawl
  from-url to-url
  the input is vast!

page-rank output is the "importance" of each page
  based on whether other important pages point to it
  really models estimated probability that someone will visit each page
  user model:
    85% chance of following a link from current page
    15% chance of visiting a random page

page-rank algorithm
  iterative, essentially simulates multiple rounds of users clicking links
  ranks (probabilities) gradually converge
  page-rank would be awkward and slow in MapReduce

my example input -- file "in":
  u1 u3
  u1 u1
  u2 u3
  u2 u2
  u3 u1
  
I'll run page-rank in Spark (local machine, not a cluster):
  ./bin/run-example SparkPageRank in 10
  u2 has rank:  0.2610116705534049 .
  u3 has rank:  0.9999999999999998 .
  u1 has rank:  1.7389883294465944 .

apparently u1 is the most important page.

let's run some of the page-rank code in the Scala interpreter
    ./bin/spark-shell

    val lines = spark.read.textFile("in").rdd
      -- what is lines? does it contain the content of file "in"?
    lines.collect()
      -- lines yields a list of strings, one per line of input
      -- if we run lines.collect() again, it re-reads file "in"
    val links1 = lines.map{ s => val parts = s.split("\\s+"); (parts(0), parts(1)) }
    links1.collect()
      -- map, split, tuple -- acts on each line in turn
      -- parses each string "x y" into tuple ( "x", "y" )
    val links2 = links1.distinct()
      -- distinct() sorts or hashes to bring duplicates together
    val links3 = links2.groupByKey()
      -- groupByKey() sorts or hashes to bring instances of each key together
    val links4 = links3.cache()
      -- cache() == persist in memory
    var ranks = links4.mapValues(v => 1.0)

    -- now for first loop iteration
    val jj = links4.join(ranks)
      -- the join brings each page's link list and current rank together
    val contribs = jj.values.flatMap{ case (urls, rank) => urls.map(url => (url, rank / urls.size)) }
      -- for each link, the "from" page's rank divided by number of its links
    ranks = contribs.reduceByKey(_ + _).mapValues(0.15 + 0.85 * _)
      -- sum up the links that lead to each page

    -- second loop iteration
    val jj2 = links4.join(ranks)
      -- join() brings together equal keys; must sort or hash
    val contribs2 = jj2.values.flatMap{ case (urls, rank) => urls.map(url => (url, rank / urls.size)) }
    ranks = contribs2.reduceByKey(_ + _).mapValues(0.15 + 0.85 * _)
      -- reduceByKey() brings together equal keys

    -- the loop &c just creates a lineage graph.
    -- it does not do any real work.

    val output = ranks.collect()
      -- collect() is an action.
      -- it causes the whole computation to execute!
    output.foreach(tup => println(s"${tup._1} has rank:  ${tup._2} ."))

until the final collect(), this code just creates a lineage graph
  it does not process the data

what does the lineage graph look like?
  Figure 3
  it's a graph of transform stages -- a data-flow graph
  it's a complete recipe for the computation
  note that the loop added to the graph -- there is not actually a cycle
    there's a *new* ranks/contribs for each loop iteration

for multi-step computation, this programming model is more convenient than MapReduce

the Scala code runs in the "driver" machine of Figure 2
  the driver constructs a lineage graph
  the driver compiles Java bytecodes and sends them to worker machines
  the driver then manages execution and data movement

what does the execution look like?
  [diagram: driver, partitioned input file, workers]
  * input in HDFS (like GFS)
  * input data files are already "partitioned" over many storage servers
    first 1,000,000 lines in one partition, next lines in another, &c.
  * more partitions than machines, for load balance
  * each worker machine takes a partition, applies lineage graph in order
  * when computation on different partitions is independent ("narrow"):
    no inter-machine communication required after first read
    a worker applies series of transformations to input stream

this is already more efficient than MapReduce
  data is forwarded directly from one transformation to the next
  MR would need multiple Map+Reduces
    with expensive store to GFS, then re-read, between each

what about distinct()? groupByKey()? join()? reduceByKey()?
  these need to look at data from *all* partitions, not just one
  because all records with a given key must be considered together
  these are the paper's "wide" dependencies (as opposed to "narrow")

how are wide dependencies implemented?
  [diagram]
  a lot like Map intermediate output in MapReduce
  the driver knows where the wide dependencies are
    e.g. between the map() and the distinct() in page-rank
    upstream transformation, downstream transformation
  the data must be "shuffled" into new partitions
    e.g. bring all of a given key together
  after the upstream transformation:
    split output up by shuffle criterion (typically some key)
    arrange into buckets in memory, one per downstream partition
  before the downstream transformation:
    (wait until upstream transformation completes -- driver manages this)
    each worker fetches its bucket from each upstream worker
    now the data is partitioned in a different way
  wide is expensive!
    all data is moved across the network
    it's a barrier -- all workers must wait until all are done

what if data is re-used?
  e.g. links4 in our page-rank
  by default, must be re-computed, e.g. re-read from input file
  persist() and cache() cause links to be saved in memory for re-use

re-using persisted data is another big advantage over MapReduce

Spark can optimized based on its view of the whole lineage graph
  stream records, one at a time, though sequence of narrow transformations
    increases locality, good for CPU data caches
    avoids having to store entire partition of records in memory
  notice when shuffles aren't needed b/c inputs already partitioned in the same way
    e.g. links4.join(ranks)

what about fault tolerance?
  what if one machine crashes?
  its memory and computation state are lost
  driver re-runs transformations on crashed machine's partitions on other machines
    usually each machine is responsible for many partitions
    so load can be spread
    thus re-computation is pretty fast
  for narrow dependencies, only lost partitions have to be re-executed

what about failures when there are wide dependencies?
  re-computing one failed partition requires information from *all* partitions
  so *all* partitions may need to re-execute from the start!
    even though they didn't fail
  Spark supports checkpoints to HDFS (like GFS) to cope with this
    driver only has to recompute along lineage from latest checkpoint
  for page-rank, perhaps checkpoint ranks every 10th iteration

limitations?
  geared up for batch processing of bulk data
  all records treated the same way
  transformations are "functional" -- turn input into output
    no notion of modifying data in place

summary
  Spark improves expressivity and performance vs MapReduce
  giving the framework a view of the complete dataflow is helpful
    performance optimizations
    failure recovery
  what were the keys to performance?
    leave data in memory between transformations, vs write to GFS then read
    re-use of data in memory (e.g. links in page-rank)
  Spark very successful, widely used
讲者备注：
(无)

# Transcript

[00:00:00] We'll talking about is Spark,
[00:00:04] and so this goes back to almost the beginning of the semester,
[00:00:08] where you know we talked quite a bit about mapreduce,
[00:00:11] in fact you know lab 1 that you implemented mapreduce.
[00:00:15] So and really what's you know sort of informally,
[00:00:20] you know the Spark is basically the successor you know to Hadoop,
[00:00:29] and Hadoop is the open-source version of mapreduce.
[00:00:39] So I think today,
[00:00:41] you know people typically will use Spark instead of Hadoop.
[00:00:47] And so it's really widely used,
[00:00:53] and it's widely used for data science computation,
[00:00:56] so people that you know have lots of data,
[00:00:59] that need to run some computation over it,
[00:01:01] require a ton of machines,
[00:01:03] you know Spark is designed for that particular case,
[00:01:07] it is commercialized by a company called Databricks,
[00:01:12] you know Matei Zaharia who is the author,
[00:01:16] of the main author of this paper, his PhD thesis,
[00:01:19] started with a number of other people,
[00:01:21] this company Databricks which commercializes Spark,
[00:01:26] but it also supports the Apache open-source Spark version,
[00:01:33] it's a pretty popular open-source project
[00:01:36] or very popular open-source project.
[00:01:39] It is one reason as sort of replaced you know the use of Hadoop is,
[00:01:44] because it actually supports wider range,
[00:01:48] wider range of applications than mapreduce can,
[00:01:52] in particular you know very good at these iterative.
[00:01:57] Oops, what happened there,
[00:02:01] let me see, something maybe external crash,
[00:02:04] hold on a second.
[00:02:36] Okay, fortunately having good shape.
[00:02:42] Okay, so it supports a wide range of applications,
[00:02:46] in particular is good at these iterative applications,
[00:02:48] so applications where multiple rounds of mapreduce operations,
[00:02:52] so if you have an application,
[00:02:53] that requires one set of mapreduce
[00:02:55] followed by another set of mapreduce, followed by another mapreduce computation,
[00:02:59] Spark is really good at it,
[00:03:00] and the reason is so good at it is,
[00:03:02] because basically it keeps the intermediate results in memory,
[00:03:05] and that's really good support, programming support for doing so.
[00:03:09] In some ways, you know,
[00:03:14] so if there's any connection at all between the previous paper and this paper,
[00:03:17] which basically is not,
[00:03:18] but you know they're all both targeted to sort of in-memory computations,
[00:03:28] you know for datasets that basically fit in-memory,
[00:03:31] in the previous paper and FaRM paper
[00:03:33] is all about you know the database fit in-memory,
[00:03:35] here's the dataset the data science computation,
[00:03:38] for data science computation that you want to do.
[00:03:43] Of course, you know since 2012, when this paper was published,
[00:03:46] a lot of things have happened,
[00:03:48] the Spark is not really tied to Scala,
[00:03:53] sort of described in the paper,
[00:03:55] but there are other language front ends, for example,
[00:03:57] but probably more importantly,
[00:03:59] you know the RDD has defined in this paper slightly deprecated,
[00:04:05] and replace by dataframes,
[00:04:10] but dataframes the way to think about it,
[00:04:13] the way I think about it,
[00:04:14] this is basically column, is RDD with explicit columns,
[00:04:19] and all the good ideas of RDDs are also true for dataframes.
[00:04:24] And so for the rest of the lecture,
[00:04:26] I'm just gonna talk about RDDs,
[00:04:28] and think about them as equivalently as to dataframes.
[00:04:37] Any questions before I proceed?
[00:04:46] And then quick other point,
[00:04:48] maybe this research is really quite successful,
[00:04:52] widely used,
[00:04:54] also Matei you know received the ACM doctoral thesis award,
[00:05:00] for this for this thesis, that basically is all about Spark.
[00:05:04] So it's quite unusual actually for a doctoral thesis,
[00:05:08] that to have that kind of impact.
[00:05:16] Okay?
[00:05:19] So the way I want to talk about Spark
[00:05:22] by just looking at the some of examples,
[00:05:25] because I think you get the best,
[00:05:27] you, you understand the programming model,
[00:05:32] but that is based on RDDs,
[00:05:34] best by this I think looking at examples,
[00:05:40] and you get the best idea of what actually in RDD is.
[00:05:44] And so let me pull up some of the examples,
[00:05:46] that were in the paper,
[00:05:48] then we'll walk through those.
[00:05:51] Take this one,
[00:05:54] so let's start here, a very simple example.
[00:05:59] And so you know the idea is that,
[00:06:03] in this example is that,
[00:06:05] you know first of all, you can sort of use spark interactively,
[00:06:09] so you can see that start of Spark at the workstation or laptop,
[00:06:13] and start interacting with Spark,
[00:06:17] you know the way you know you type in commands like this.
[00:06:21] And so now what does this command do,
[00:06:24] you know this basically creates an RDD,
[00:06:27] you know called this RDDs, lines is an RDD,
[00:06:31] and I just represents you know the RDD that actually stored in HDFS,
[00:06:36] and you know the HDFS you know might be have many partitions
[00:06:41] you know for this particular file,
[00:06:43] so for example the first thousand or million records live on like partition 1,
[00:06:48] there's next million live in partition 2,
[00:06:50] and the next million live to partition 3,
[00:06:53] then this RDD lines basically represents you know that that set of partitions,
[00:07:00] when you run this line or you type in this line in return, hit return,
[00:07:05] basically nothing really happens,
[00:07:07] and so this is what the paper refers to as lazy computations,
[00:07:12] in fact, computations executed some point later,
[00:07:14] and we'll see in a second when,
[00:07:16] but at this particular point,
[00:07:18] the only thing that's actually happened is that,
[00:07:20] there is a lines lines object that happens to be in RDD.
[00:07:25] And RDD you know supports a wide range of operations,
[00:07:30] we can actually look a little bit with some operations that it supports.
[00:07:37] Yeah, so it's has, an RDD has an API,
[00:07:42] and it turns out that the methods of the API
[00:07:44] or the methods on RDD fail in two classes,
[00:07:47] one are the, are Actions,
[00:07:51] and Actions are really the operations that will actually cause computation to happen,
[00:07:56] so all the lazily sort of build up computation
[00:07:59] really happens at the point that you're run an Action,
[00:08:01] and so for example, you run count or collect,
[00:08:04] then, the Spark computation actually is executed.
[00:08:10] All the other API or methods are Transformations,
[00:08:15] and they basically take one RDD and turn it into another RDD,
[00:08:19] it turns out that every RDD is actually read-only or immutable,
[00:08:23] so you can't modify an RDD,
[00:08:27] can only basically generate new RDDs form an existing one.
[00:08:32] And so if we look at the second line,
[00:08:34] this basically creates a second RDD, RDD errors,
[00:08:38] and that one is created by running a filter on the lines RDD,
[00:08:44] so the lines RDD read-only, read-only,
[00:08:49] you can run this method the filter on it,
[00:08:51] which in this case,
[00:08:52] basically filters out all the records start with,
[00:08:55] all the lines that start with the message ERROR or string ERROR,
[00:08:59] and that represents a new RDD,
[00:09:01] and again at this point, nothing actually really being computed,
[00:09:04] is just like a recipe or you know built some almost a dataflow
[00:09:08] or what the paper calls iterative graph of the computation.
[00:09:22] Also, when the computation actually starts running,
[00:09:26] it hasn't run yet,
[00:09:26] but when it will start running,
[00:09:28] these operations are pipelined,
[00:09:31] with that, they mean,
[00:09:33] so for example in stage one you know the this computation of the lines,
[00:09:38] the stage one will read some set of records,
[00:09:41] you know from for example this first partition,
[00:09:44] and then, do its processing on it,
[00:09:48] if there's anything and then handed off to stage two,
[00:09:51] you know what stage two basically you know doing this filter,
