# 整理任务

- 课程：MIT 6.824 Distributed Systems (Spring 2021)
- 本讲：Lecture 9 - Zookeeper
- 片段：00:00:00-00:10:00
- YouTube 章节：未提供
- 材料模式：结合 transcript 与课件

请按下面结构输出 Markdown：

## 本段在整讲中的作用
说明老师此处试图解决什么问题，以及它与前文的连接。无法从片段判断前文时明确说明。

## 教学展开
严格按老师实际讲解顺序记录。保留“为什么提出 -> 如何定义 -> 怎样推导/解释 -> 如何举例 -> 得出什么结论”的链条。

## 概念、符号与推导
逐项记录定义、符号、公式、前提和推导步骤。没有公式时写“本段无公式推导”，不要自行补充。

## 例子与课堂提示
记录老师的例子、类比、演示、反例、经验判断和易错点，并说明它们服务于哪个知识点。

## 本段掌握检查
列出 3-5 个可用 transcript 和课件回答的问题，最后给出简短参考答案。

## 待核对项
列出字幕疑点、课件与口述差异或缺失上下文；没有则写“无”。

# 课件材料

--- [课件: l-zookeeper.txt p.1] ---
标题：6.824 2020 Lecture 8: Zookeeper Case Study
正文：
6.824 2020 Lecture 8: Zookeeper Case Study

Reading: "ZooKeeper: wait-free coordination for internet-scale systems", Patrick
Hunt, Mahadev Konar, Flavio P. Junqueira, Benjamin Reed.  Proceedings of the 2010
USENIX Annual Technical Conference.

What questions does this paper shed light on?
  * Can we have coordination as a stand-alone general-purpose service?
    What should the API look like?
    How can other distributed applications use it?
  * We paid lots of money for Nx replica servers.
    Can we get Nx performance from them?

First, performance.
  For now, view ZooKeeper as some service replicated with a Raft-like scheme.
  Much like Lab 3.
  [clients, leader/state/log, followers/state/log]

Does this replication arrangement get faster as we add more servers?
  Assume a busy system, lots of active clients.
  Writes probably get slower with more replicas!
    Since leader must send each write to growing # of followers.
  What about reads?

Q: Can replicas serve read-only client requests form their local state?
   Without involving the leader or other replicas?
   Then total read capacity would be O(# servers), not O(1)!

Q: Would reads from followers be linearizable?
   Would reads always yield fresh data?
   No:
     Replica may not be in majority, so may not have seen a completed write.
     Replica may not yet have seen a commit for a completed write.
     Replica may be entirely cut off from the leader (same as above).
   Linearizability forbids stale reads!

Q: What if a client reads from an up-to-date replica, then a lagging replica?
   It may see data values go *backwards* in time! Also forbidden.

Raft and Lab 3 avoid these problems.
  Clients have to send reads to the leader.
  So Lab 3 reads are linearizable.
  But no opportunity to divide the read load over the followers.
  
How does ZooKeeper skin this cat?
  By changing the definition of correctness!
  It allows reads to yield stale data.
  But otherwise preserves order.

Ordering guarantees (Section 2.3)
  * Linearizable writes
    clients send writes to the leader
    the leader chooses an order, numbered by "zxid"
    sends to replicas, which all execute in zxid order
    this is just like the labs
  * FIFO client order
    each client specifies an order for its operations (reads AND writes)
    writes:
      writes appear in the write order in client-specified order
      this is the business about the "ready" file in 2.3
    reads:
      each read executes at a particular point in the write order
      a client's successive reads execute at non-decreasing points in the order
      a client's read executes after all previous writes by that client
        a server may block a client's read to wait for previous write, or sync()

Why does this make sense?
  I.e. why OK for reads to return stale data?
       why OK for client 1 to see new data, then client 2 sees older data?

At a high level:
  not as painful for programmers as it may seem
  very helpful for read performance!

Why is ZooKeeper useful despite loose consistency?
  sync() causes subsequent client reads to see preceding writes.
    useful when a read must see latest data
  Writes are well-behaved, e.g. exclusive test-and-set operations
    writes really do execute in order, on latest data.
  Read order rules ensure "read your own writes".
  Read order rules help reasoning.
    e.g. if read sees "ready" file, subsequent reads see previous writes.
         (Section 2.3)
         Write order:      Read order:
         delete("ready")
         write f1
         write f2
         create("ready")
                           exists("ready")
                           read f1
                           read f2
         even if client switches servers!
    e.g. watch triggered by a write delivered before reads from subsequent writes.
         Write order:      Read order:
                           exists("ready", watch=true)
                           read f1
         delete("ready")
         write f1
         write f2
                           read f2

A few consequences:
  Leader must preserve client write order across leader failure.
  Replicas must enforce "a client's reads never go backwards in zxid order"
    despite replica failure.
  Client must track highest zxid it has read
    to help ensure next read doesn't go backwards
    even if sent to a different replica

Other performance tricks in ZooKeeper:
  Clients can send async writes to leader (async = don't have to wait).
  Leader batches up many requests to reduce net and disk-write overhead.
    Assumes lots of active clients.
  Fuzzy snapshots (and idempotent updates) so snapshot doesn't stop writes.

Is the resulting performance good?
  Table 1
  High read throughput -- and goes up with number of servers!
  Lower write throughput -- and goes down with number of servers!
  21,000 writes/second is pretty good!
    Maybe limited by time to persist log to hard drives.
    But still MUCH higher than 10 milliseconds per disk write -- batching.

The other big ZooKeeper topic: a general-purpose coordination service.
  This is about the API and how it can help distributed s/w coordinate.
  It is not clear what such an API should look like!

What do we mean by coordination as a service?
  Example: VMware-FT's test-and-set server
    If one replica can't talk to the other, grabs t-a-s lock, becomes sole server
    Must be exclusive to avoid two primaries (e.g. if network partition)
    Must be fault-tolerant
  Example: GFS (more speculative)
    Perhaps agreement on which meta-data replica should be master
    Perhaps recording list of chunk servers, which chunks, who is primary
  Other examples: MapReduce, YMB, Crawler, etc.
    Who is the master; lists of workers; division of labor; status of tasks
  A general-purpose service would save much effort!

Could we use a Lab 3 key/value store as a generic coordination service?
  For example, to choose new GFS master if multiple replicas want to take over?
  perhaps
    Put("master", my IP address)
    if Get("master") == my IP address:
      act as master
  problem: a racing Put() may execute after the Get()
    2nd Put() overwrites first, so two masters, oops
    Put() and Get() are not a good API for mutual exclusion!
  problem: what to do if master fails?
    perhaps master repeatedly Put()s a fresh timestamp?
    lots of polling...
  problem: clients need to know when master changes
    periodic Get()s?
    lots of polling...

Zookeeper API overview (Figure 1)
  the state: a file-system-like tree of znodes
  file names, file content, directories, path names
  typical use: configuration info in znodes
    set of machines that participate in the application
    which machine is the primary
  each znode has a version number
  types of znodes:
    regular
    ephemeral
    sequential: name + seqno

Operations on znodes (Section 2.2)
  create(path, data, flags)
    exclusive -- only first create indicates success
  delete(path, version)
    if znode.version = version, then delete
  exists(path, watch)
    watch=true means also send notification if path is later created/deleted
  getData(path, watch)
  setData(path, data, version)
    if znode.version = version, then update
  getChildren(path, watch)
  sync()
    sync then read ensures writes before sync are visible to same client's read
    client could instead submit a write

ZooKeeper API well tuned to synchronization:
  + exclusive file creation; exactly one concurrent create returns success
  + getData()/setData(x, version) supports mini-transactions
  + sessions automate actions when clients fail (e.g. release lock on failure)
  + sequential files create order among multiple clients
  + watches -- avoid polling

Example: add one to a number stored in a ZooKeeper znode
  what if the read returns stale data?
    write will write the wrong value!
  what if another client concurrently updates?
    will one of the increments be lost?
  while true:
    x, v := getData("f")
    if setData(x + 1, version=v):
      break
  this is a "mini-transaction"
    effect is atomic read-modify-write
  lots of variants, e.g. test-and-set for VMware-FT

Example: Simple Locks (Section 2.4)
  acquire():
    while true:
      if create("lf", ephemeral=true), success
      if exists("lf", watch=true)
        wait for notification

  release(): (voluntarily or session timeout)
    delete("lf")

  Q: what if lock released just as loser calls exists()?

Example: Locks without Herd Effect
  (look at pseudo-code in paper, Section 2.4, page 6)
  1. create a "sequential" file
  2. list files
  3. if no lower-numbered, lock is acquired!
  4. if exists(next-lower-numbered, watch=true)
  5.   wait for event...
  6. goto 2

  Q: could a lower-numbered file be created between steps 2 and 3?
  Q: can watch fire before it is the client's turn?
  A: yes
     lock-10 <- current lock holder
     lock-11 <- next one
     lock-12 <- my request

     if client that created lock-11 dies before it gets the lock, the
     watch will fire but it isn't my turn yet.

Using these locks
  Different from single-machine thread locks!
    If lock holder fails, system automatically releases locks.
    So locks are not really enforcing atomicity of other activities.
    To make writes atomic, use "ready" trick or mini-transactions.
  Useful for master/leader election.
    New leader must inspect state and clean up.
  Or soft locks, for performance but not correctness
    e.g. only one worker does each Map or Reduce task (but OK if done twice)
    e.g. a URL crawled by only one worker (but OK if done twice)

ZooKeeper is a successful design.
  see ZooKeeper's Wikipedia page for a list of projects that use it
  Rarely eliminates all the complexity from distribution.
    e.g. GFS master still needs to replicate file meta-data.
    e.g. GFS primary has its own plan for replicating chunks.
  But does bite off a bunch of common cases:
    Master election.
    Persistent master state (if state is small).
    Who is the current master? (name service).
    Worker registration.
    Work queues.
  
Topics not covered:
  persistence
  details of batching and pipelining for performance
  fuzzy snapshots
  idempotent operations
  duplicate client request detection

References:
  https://zookeeper.apache.org/doc/r3.4.8/api/org/apache/zookeeper/ZooKeeper.html
  ZAB: http://dl.acm.org/citation.cfm?id=2056409
  https://zookeeper.apache.org/
  https://cs.brown.edu/~mph/Herlihy91/p124-herlihy.pdf  (wait free, universal
  objects, etc.)
讲者备注：
(无)

# Transcript

[00:00:00] Thank you.
[00:00:01] Okay, good afternoon, good morning, good evening, good night, wherever you are,
[00:00:06] so today I want to talk about the zookeeper
[00:00:10] and with the this background,
[00:00:13] paper that we you know which is assigned for today,
[00:00:15] which is from 2010.
[00:00:18] And, so there's gonna be referring back the last couple lectures,
[00:00:24] I think we dove in quite a bit of detail into raft,
[00:00:26] including looking at code,
[00:00:30] the lectures from now on are going to be more conceptual
[00:00:33] and exploring ideas in distributed systems zookeeper's,
[00:00:39] and the zookeeper, what is particularly relevant to us,
[00:00:43] because it has a little bit of relationship
[00:00:46] as we'll see with lab 3.
[00:00:49] And so it will allow us to talk about some properties of lab 3,
[00:00:52] and particularly linearizability,
[00:00:55] but zookeeper system more importantly,
[00:00:58] it's interesting,
[00:00:58] because one of widely-used system in practice,
[00:01:08] well beyond Apache, well beyond Yahoo,
[00:01:12] is a open source Apache project,
[00:01:15] that is still active.
[00:01:18] What particularly interesting about it for us,
[00:01:22] in the lecture like today is actually high performance,
[00:01:25] and high performance, I mean,
[00:01:27] much higher performance than what actually lab 3 is gonna be.
[00:01:33] I'll talk to talk quite a bit of detail about this,
[00:01:36] and there's sort of two reasons why it is high performance,
[00:01:39] one, it is the client operations are asynchronous,
[00:01:47] and really what high performing from here means is,
[00:01:50] we can, the system can process many more operations per second,
[00:01:54] so it's really a throughput metric,
[00:01:56] and the second reason it's high performances because,
[00:02:00] it doesn't provide strong consistency,
[00:02:03] so it doesn't, it has an interesting consistency definition,
[00:02:12] and it gives us some freedom
[00:02:13] to execute basically read operations on any replica,
[00:02:16] and therefore read [] can scale.
[00:02:20] Then, the second aspect that is interesting from zookeeper is,
[00:02:23] in addition to being high performance,
[00:02:25] it is sort of a generic,
[00:02:26] what they call coordination service,
[00:02:35] and the point being here is,
[00:02:37] there are many applications,
[00:02:40] where you need to keep track of like who's part of this cluster
[00:02:45] and who's the master,
[00:02:47] so think about the mapreduce or think about the GFS,
[00:02:51] the master in GFS needs to keep track
[00:02:53] or like for every chunk,
[00:02:55] who are the servers that actually server that,
[00:02:57] [] server chunk who servers actually is the master,
[00:03:01] and so tracking that sort of configuration information,
[00:03:04] you know comes up in lots of different distributed applications,
[00:03:07] zookeeper is really designed to support that kind of thing,
[00:03:10] so you sort of outsource all the configuration management to zookeeper,
[00:03:15] and then focus the rest of your application development,
[00:03:19] on other aspects of your distributed system.
[00:03:23] Okay, so that's sort of a brief introduction,
[00:03:25] you know the topics that we're gonna be talking about.
[00:03:30] And as usual, feel free to interrupt at any time,
[00:03:33] or you know post something in the chat message,
[00:03:37] let me actually pull up the chat.
[00:03:41] Okay, so we just you know start from the basics zookeeper is replicated state machine,
[00:03:59] in the same way that you know the replicated state machines that we've been seeing.
[00:04:03] And so let's draw the usual picture,
[00:04:06] we have some servers,
[00:04:08] in this case,
[00:04:08] it's gonna be zookeeper ZK.
[00:04:12] It receives requests from clients,
[00:04:15] that create create znode,
[00:04:18] and basically the way it interacts to distribute these operations,
[00:04:26] it has sort of a separate library,
[00:04:27] you know think about this as the raft library,
[00:04:30] in their case, it's called ZAB.
[00:04:35] Basically, you know the leader you know sticks the operation into
[00:04:40] you know the equivalent raft library,
[00:04:42] that you know talks you know to you know other libraries or other peers,
[00:04:49] that basically creates a log,
[00:04:53] you know that conveys with all these operations and all these machines,
[00:04:57] and then the operations are the log of feedback to the [],
[00:05:01] so in our case, the apply channel to the service,
[00:05:05] the service applies the operation in response to the client,
[00:05:07] and so we have basically different [] is running.
[00:05:11] And so far, you know in the labs you know really focusing mostly,
[00:05:17] you know lab 2 is all about you know this part,
[00:05:21] implementing in our case raft, so instead of ZAB,
[00:05:25] and for the first or the at a very high level,
[00:05:28] you can think about ZAB being another raft,
[00:05:32] you know providing sort of similar guarantees
[00:05:34] you know implemented quite differently,
[00:05:37] you know it provides an order of all the operations,
[00:05:41] the [] failures, network partitions, etc etc,
[00:05:45] doesn't suffer from split-brain brain,
[00:05:47] all are sort of things which we associate with the raft library.
[00:05:52] In you know what we're gonna be focusing in lab 3 on is,
[00:05:57] actually implementing a service on top of it,
[00:05:59] so this paper talks about the this coordination service zookeeper in,
[00:06:04] we're going to actually implement in lab 3 key value stores,
[00:06:12] and the data structure is just map from keys to values.
[00:06:17] And so the operations you know that we're going to be supporting are put and get,
[00:06:23] so the clients may put and get operations you know to the service,
[00:06:26] the service runs in through raft,
[00:06:29] and then applies them one by one to the key value store.
[00:06:33] In zookeeper you know the structures slightly different,
[00:06:36] there's a tree of znodes,
[00:06:38] but the basic operation is the applys the same,
[00:06:41] you know the lower layer the ZAB or the ZAB library
[00:06:45] or ZAB library orders all the operations,
[00:06:47] they're applied in the same order all replicas,
[00:06:50] because they are applied all in the same order,
[00:06:52] there's no non-determinism,
[00:06:54] the resulting state on each of the replica is going to be identical.
[00:07:00] Okay?
[00:07:02] That's sort of the basic setting of this paper,
[00:07:07] and sort of also the relation, part of the relationship
[00:07:09] between lab 3 and the zookeeper service itself.
[00:07:14] And mostly going to be talking in this lecture
[00:07:16] on focus on the zookeeper part itself,
[00:07:18] and not talk about ZAB,
[00:07:20] because you're gonna assume,
[00:07:22] that's similar to what we're doing and we're doing in lab 2.
[00:07:28] Okay, we're going to talking a little bit about,
[00:07:31] what kind of performance would you observe,
[00:07:35] if you want to finish lab 3,
[00:07:38] and would measure how many you puts or gets your operation get through per second,
[00:07:43] because one of the achievements this paper is you know high performance.
[00:07:48] So let's think a little bit about that,
[00:07:50] so let's assume you know a put operation coming in,
[00:07:54] and you know put operation comes in by the leader,
[00:07:56] and so we're just gonna go for the normal case,
[00:07:58] like the standard case, where everything is working,
[00:08:00] no network failures, no partitions, nothing,
[00:08:02] everything works out perfectly.
[00:08:04] Here's the leader,
[00:08:05] we've got two followers.
[00:08:09] And you know now you know that protocol you know probably inside out,
[00:08:14] you know the first thing of course it happens,
[00:08:15] like would be the leader call start,
[00:08:18] start actually writes the put operation to its, to log in the leader,
[00:08:26] and then the leader you know propagates it's log to the other followers,
[00:08:31] and you know before it actually happens in parallel,
[00:08:35] send that argument,
[00:08:36] let me draw it slightly differently,
[00:08:40] basically it launches a bunch of RPCs almost instantaneously to the different followers,
[00:08:47] each follower of course you know will append the entry to its logs,
[00:08:50] so that requires a writes to persistent storage,
[00:08:54] same thing here,
[00:08:55] and then they respond back.
[00:08:57] And so here's a response back,
[00:08:59] and in this case, majority has responded,
[00:09:03] the leader can actually apply the operation,
[00:09:05] so the leader will actually apply the operation here for [],
[00:09:10] so do the put and send the response back to the client.
[00:09:16] What curious about is like,
[00:09:17] how many put per second can we actually get,
[00:09:21] you know in this in this setting.
[00:09:26] And you know think about it,
[00:09:28] you know the when you do [] back-of-the-envelope-calculation,
[00:09:33] we don't really care exactly about the exact numbers,
[00:09:36] but you know one roundtrip,
[00:09:38] we need at least one roundtrip to actually get the majority,right,
[00:09:42] leader needs to talk to at least one follower to actually maintain the majority,
[00:09:45] so we're going to have at least one roundtrip messaging.
[00:09:52] And then you sort of need to look at the writes to stable storage,
[00:09:58] because those tend to be expensive,
